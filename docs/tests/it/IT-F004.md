---
status: Approved
feature: F004
title: 宮沢賢治追加3作品・お気に入り 結合試験仕様
updated: 2026-07-27
---

# 結合試験仕様書 F004（宮沢賢治追加3作品・お気に入り）

## 1. 試験方針

F004の承認候補、v0.3.0二重baseline、同一作者merge、原典・review・音声、作品単位atomic受入、お気に入りcontroller、6 route、exact release/rollbackを実moduleとcanonical artifactで接続する。失敗時は旧public・利用者閲覧・第三者変更を保護し、手動確認を必須条件にしない。

## 2. 結合シナリオ

### IT-F004-001: verified承認contextからF004 manifestを生成 [DES-F004-001][DES-F004-011]

- **構成**: canonical definition/policy descriptor、Q-022/Q-023、Approved SRS/QT、in-review CHG、candidate registry、既存宮沢Catalog。
- **手順**: descriptor SHA・queue・文書・identityを検証し、ApprovedBatchContextからmanifestを生成する。policy/authorExpectation差替え、reparse、宮沢identity各field差を投入する。
- **期待結果**: オツベルと象→雪渡り→カイロ団長の3 workが`pending`の`draft`となる。不正時はbrandをmintせずregistry/public不変。

### IT-F004-002: v0.3.0二重baselineと差分allowlistを全段階で検証 [DES-F004-002]

- **構成**: release `79d1282…`、control `5a1a06a…`、work preview、final tree、dist、F004 application allowlist。
- **手順**: 3作者・9作品・472台詞・463音声、既存content/media、published F003 controlを復元し、追跡済みJS/CSS差分、未追跡差分、既存1 byte差を試す。
- **期待結果**: 既存projection exact＋追跡済みapplication差分だけPASS。release/control混同、未追跡差分、既存改変はblockedで修復しない。

### IT-F004-003: 権利観測から原典固定まで [DES-F004-003]

- **構成**: production transport、公式CSV/作家page/3図書カード/XHTML、selection/predeploy context。
- **手順**: 3作品を直列取得し権利・版・URL・charset・書誌・selectorを照合する。SSRF/TLS/size/time境界とpredeploy driftを投入する。
- **期待結果**: 正規3作品だけSourceRecordへatomic固定し、危険通信・差分時は旧sourceを維持して停止する。

### IT-F004-004: 抽出・独立二判定・裁定・読み補正を完結 [DES-F004-004]

- **構成**: 固定3 XHTML、candidate、trusted authorization、primary/secondary/adjudicator、revision。
- **手順**: 同一入力2回抽出、独立seal、semantic照合、不一致裁定、revision適用を行い、role/work横断・期限切れ・pendingを注入する。
- **期待結果**: 外側`「」`だけ同hashで抽出され、全candidateがapproved/rejectedへ確定、pending 0。display不変でspeechを再現し、違反時は音声へ進めない。

### IT-F004-005: safety・voice diff・forecast/actual容量を同一tupleへ結合 [DES-F004-005][DES-F004-011]

- **構成**: approved speech、v0.3.0校正profile/config/cache、loopback VOICEVOX、Git/disk adapter。
- **手順**: 候補3境界、5容量区分、cache hit/miss、生成失敗を試し、相対pathのplanからmissだけ生成してactualを再計測する。
- **期待結果**: 同値pass/+1 blocked、警告域を区別し、approved missだけ最大同時1で生成。失敗時stage破棄、既存audio/public不変。

### IT-F004-006: previewから3作品を順次atomic受入してcrash回復 [DES-F004-006][DES-F004-007][DES-F004-011]

- **構成**: generic WorkPreview projector、canonical evidence、manifest、journal、filesystem fault。
- **手順**: 先行accepted＋現在staged 1件でnon-destructive previewを作り、3作品を順番にprepare/promoteする。preview/final brand混同、順序違反、各journal phase停止を試す。
- **期待結果**: preview hashを再検算した完了workだけaccepted。旧版/完成新版へ決定的回復し、previewをfinalへ昇格せず第三者変更を上書きしない。

### IT-F004-007: 同一宮沢作者へ3作品をデータ駆動統合 [DES-F004-007]

- **構成**: v0.3.0 Catalog、FinalCatalogFragment、既存宮沢画像/provenance、introduce/reuse fixture。
- **手順**: baseline identity joinからreuseを導出しFinalCatalogへmergeする。join 0/2、identity差、新画像、別作者work、F003新作者fixtureも試す。
- **期待結果**: 宮沢だけ3→6作品、他作者各3、authors/artwork新規0。generic loaderは検証済み新作者fixtureだけintroduceし、F004では拒否する。

### IT-F004-008: 注意表示・初期全閉・単一音声UX [DES-F004-008]

- **構成**: 3作者・12作品、3配置notice/credit、AudioController、遅延event/404。
- **手順**: route遷移・直アクセス・再読込・back/forward、pointer/keyboard展開、再生・route切替を行う。
- **期待結果**: 通常入口は全作品open 0、利用者操作だけ展開。抜粋/原作由来/書誌をtext node表示し、同時再生1、404個別隔離。

### IT-F004-009: FavoriteControllerを全画面で共有し端末内永続化 [DES-F004-009][DES-F004-010]

- **構成**: application mount、Author/Favorites renderer、FavoriteController、StorageLike、既存/F004台詞。
- **手順**: 全作者routeを巡回してCatalog全dialogue IDとDOM favorite controlのID/count/uniqueを照合した後、pointer/keyboard toggle、route移動、再読込、複数subscriber、解除、disposeを行う。保存反映は操作直後の単発読取りではなく、既定timeout内の`expect.poll`相当で期待snapshotを待ってからschemaを同期assertする。
- **期待結果**: 全dialogueとcontrolが重複・欠落0の1対1で初期aria/labelが一致する。controller 1個のsnapshotが全画面で一致し、exact keyへIDだけcanonical保存、自動音声0、F004台詞も同処理。

### IT-F004-010: 破損・quota・未知IDでもお気に入りを安全継続 [DES-F004-009][DES-F004-012]

- **構成**: raw同値/+1、malformed/深いJSON、5001 ID、未知version/ID、getter/read/write/remove/quota fault、security checker。
- **手順**: 各保存値から起動・toggle・route描画・remountし、subscriber回数、操作前後storage bytes、memory snapshot、通信/storage key/DOMを観測する。正常保存だけはbounded pollで期待値を待ち、失敗fixtureは旧storage不変とmemory更新を別oracleで即時確認する。
- **期待結果**: malformed/未知version/field/5001/raw超過はempty、重複/未知IDは有効subsetへ正規化し同key rewrite。getter/read失敗後もmemory toggle可能。write/quota失敗後は新snapshotを通知exact 1でroute間保持、旧storage不変、remountは旧値から開始。他key・保存本文・外部送信・XSS 0。

### IT-F004-011: お気に入りrouteと元作品へのone-shot移動 [DES-F004-008][DES-F004-010][DES-F004-012]

- **構成**: 共通navigation、複数作者favorite、router、DOM参照map、Audio/Favorite controller。
- **手順**: keyboardで`#/favorites`へ移動し、一覧再生・解除・元作品移動を行う。その後reload/back/forward/直アクセスする。
- **期待結果**: Catalog順一覧・空状態・単一音声・focusが動作。明示操作時だけ対象panelを開き、intent一回消費後の全通常入口はopen 0。

### IT-F004-012: generic batch・path・preview/final契約をF003/F004 fixtureで再利用 [DES-F004-001][DES-F004-007][DES-F004-011]

- **構成**: verified F003 introduce/F004 reuse definition、candidate/fragment/preview/release command、相対path fixture、application source静的検査。
- **手順**: 同じcommandへ両definitionを投入する。production TypeScriptだけをAST解析し、`if/switch/ternary`等で`batchId|feature`をliteral比較する意味的分岐とF003 path special caseを検出する。registry/descriptor data、trace tag、test、error textは対象外にする。
- **期待結果**: 固有値は検証済みdataだけ。generic introduce/reuse、work-preview/final、FS境界解決が動作し、production applicationの意味的固有分岐・永続絶対path 0。文字列出現だけではFAILにしない。

### IT-F004-013: 6 route runtime securityと4 browser受入 [DES-F004-008][DES-F004-009][DES-F004-010][DES-F004-012]

- **構成**: clean dist、home/3作者/favorites/credits、CSP/DOM/通信/storage/secret/audit/workflow checker、4 browser・3 viewport。
- **手順**: route digestを固定し、keyboard/reduced motionを含む全matrixを巡回する。route不足/余分、危険API、外部通信、他keyを投入する。
- **期待結果**: exact 6 route、初期全閉、お気に入り、音声がPASSし、Critical/High・CSP・外部通信・未許可storage・secret違反0。

### IT-F004-014: exact release・公開smoke・rollback・finally無効化 [DES-F004-013]

- **構成**: 全work accepted、T-062、CHG done、FinalCatalog、全試験/権利/容量、hosted artifact、ゲート④、v0.3.0 rollback descriptor、deploy adapter。
- **手順**: journalをpendingへpre-arm・fsync後、prepare→clean verify→段階deploy→6 route/Catalog/画像/Range/favorite smoke→disable/read-back→CAS closed→publishedを行う。正常、deploy/rollback/disable失敗を別fixture化し、journal temp write/sync/rename/post-read、enable、deploy/smoke、disable API〜read-back、read-back〜CAS、CAS〜published、published〜returnの各境界でrelease親processを実停止する。別process cleanup/release resumeと第三者SHA/owner差も試す。
- **期待結果**: pre-arm未完了はenable/deploy 0、pendingは正規identity/SHAだけcleanupでclosed、closed-before-publishedは同一tupleだけ再開、published後restartは冪等、第三者差は停止する。正常tupleかつread-back false・journal closedだけpublished。smoke失敗は既知v0.3.0 artifactへ戻し495 files/catalog/dist/5 routeを再確認する。

### IT-F004-015: end-to-end失敗隔離と再開 [DES-F004-001][DES-F004-002][DES-F004-003][DES-F004-004][DES-F004-005][DES-F004-006][DES-F004-007][DES-F004-008][DES-F004-009][DES-F004-010][DES-F004-011][DES-F004-012][DES-F004-013]

- **構成**: candidateからreleaseまでの全stage、各stage 1故障、v0.3.0 published baseline。
- **手順**: 各故障で停止後に同じcanonical入力から再開し、manifest、stage、favorite memory、public、journal、deploymentを比較する。
- **期待結果**: 失敗stageより後を実行せず旧publicと承認artifactを保護する。原因解消後はhash chain/journalから重複生成なく再開する。

## 3. DES網羅表

| DES | IT |
|---|---|
| DES-F004-001 | IT-F004-001, IT-F004-012, IT-F004-015 |
| DES-F004-002 | IT-F004-002, IT-F004-015 |
| DES-F004-003 | IT-F004-003, IT-F004-015 |
| DES-F004-004 | IT-F004-004, IT-F004-015 |
| DES-F004-005 | IT-F004-005, IT-F004-015 |
| DES-F004-006 | IT-F004-006, IT-F004-015 |
| DES-F004-007 | IT-F004-006, IT-F004-007, IT-F004-012, IT-F004-015 |
| DES-F004-008 | IT-F004-008, IT-F004-011, IT-F004-013, IT-F004-015 |
| DES-F004-009 | IT-F004-009, IT-F004-010, IT-F004-013, IT-F004-015 |
| DES-F004-010 | IT-F004-009, IT-F004-011, IT-F004-013, IT-F004-015 |
| DES-F004-011 | IT-F004-001, IT-F004-005〜006, IT-F004-012, IT-F004-015 |
| DES-F004-012 | IT-F004-010〜011, IT-F004-013, IT-F004-015 |
| DES-F004-013 | IT-F004-014, IT-F004-015 |

## 4. 判定

- `IT-F004-001`〜`015`を全て自動実施し、FAIL/BLOCKED 0件を必須とする。
- IT結果はUT 37件、QT 16件、release commit、dist SHA、artifact digestと同一candidate tupleへ結合する。
