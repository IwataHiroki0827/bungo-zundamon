---
feature: F010
title: 梶井基次郎3作品追加 適格性試験仕様
status: Approved
version: 1.0.0
updated: 2026-08-24
---

# QT-F010 適格性試験仕様

| ID | 対応要求 | 自動化 | 手順 | 期待結果 |
|---|---|---|---|---|
| QT-F010-001 | [REQ-F010-001] | 自動 | 固定baseline（v0.9.0、release commit `2d108fd`、tag `v0.9.0`）と統合後の既存projection・挙動をexact比較する | 既存8作者27作品・台詞・音声、asset、初期全閉、お気に入りが一致する |
| QT-F010-002 | [REQ-F010-002][REQ-F010-003][REQ-F010-004] | 自動 | 候補からmanifestを生成し、重複・identity不一致fixtureを投入する | 一意の梶井基次郎と3作品（檸檬・Ｋの昇天・愛撫）だけを受理する |
| QT-F010-003 | [REQ-F010-005][REQ-F010-006] | 自動 | 公式CSV・作者ページ・card・XHTMLと3規約を選定時とpredeployの独立snapshotで照合する。全A/AAAA・接続peer pin、IPv4-mapped/special address、redirect、encoded/decoded上限、status/content-type/charset、timeoutを検査し、3作品すべての校正者欄記載ありデータで`proofreader: null`分岐が発動しないことを確認する | 権利flagなし、著者、翻訳者0、公開中、新字新仮名、版、URL、raw bytes/path/hash/取得時刻/transport証跡がrelease tupleと一致する。全用途条件が`decision: allow`の場合だけ通し、差分・未判定・SSRF/decompression異常はnetwork/file漏洩なしで停止する。既存作品のcanonical JSON bytesは不変となる |
| QT-F010-004 | [REQ-F010-007] | 自動 | QA-F010.md No.6の確定回答（検出手順は常時有効のまま適用、候補内に外字/entity差分が検出された場合のみfail-closedで停止）に基づき、3作品の固定raw bytes/charset/SHAを処理し、未定義entity検出手順および外字（`gaiji_list`／`class="gaiji"`）検出手順（file/httpのgeneral/parameter entity、billion laughs、XInclude、外部schema/stylesheet、深さ/node/属性/text上限+1を含む）を選定時と公開直前の独立snapshotへ適用する。実際に檸檬の地の文に存在する`class="gaiji"`要素1件が候補内に含まれないことを再確認し、人為的に候補内へ外字・未定義entityを混入したfixtureも投入する | raw byte/SHA不変。3作品とも地の文の外字要素は候補外にとどまり、正式抽出される候補は外字・未定義entity0件のまま決定的なcandidate ID・順序・表示文となる。混入fixtureはfail-closedで`pending`のまま停止し、network/filesystem access 0で拒否する |
| QT-F010-005 | [REQ-F010-008] | 自動 | primary/secondaryへ別principal/session/runのauthorizationを発行し、same-principal、same-session、cross-work、replay、expiry、audience、prompt/template hash不一致、結果先見、premature adjudicator、seal改変を全候補へ適用する | 独立一致または不一致digest後に発行された第三裁定だけが確定し、tokenは一回限り、最終pending 0となる |
| QT-F010-006 | [REQ-F010-009][REQ-F010-010] | 自動 | QA-F010.md No.5の確定回答（3作品とも作品固有注記UIの新規適用対象は0件）に基づき、表示/TTS revisionと3作品のCatalog notice有無をjoinし、公開直前再確認fixture（図書カード注記の新設fixture含む）を検査する | 原文、読み補正が3配置でexact表示される。3作品とも作品固有注記UIは表示されない。将来注記が新設された場合を模したfixture投入時はfail-closedで公開停止する |
| QT-F010-007 | [REQ-F010-011] | 自動 | approved/rejected混在で差分生成・再実行する | approvedだけを生成し、失敗・欠損・孤立0、同一音声を再利用する |
| QT-F010-008 | [REQ-F010-012] | 自動 | code point `500/501`、duration `120000/120001` ms、WAV `5760044/5760045` bytesと6容量区分の上限同値/+1を検査する。F005確立済みの事後検証（`beginPhase`/`endPhase`実測差分照合）方式をそのまま適用し、宣言外pathの新規出現・変更・消失fixtureを投入する | 上限同値を通し+1を停止する。`endPhase`実測差分が宣言済みmutation列と完全一致する場合だけ受理し、最大同時書込み後も5 GiB以上残る場合だけ生成する。宣言外差分は0件を維持する |
| QT-F010-009 | [REQ-F010-002][REQ-F010-013] | 自動 | 1作品1sessionで3作品を、`pending → extracted → reviewed → budget-approved → voiced → accepted`の順に、全状態の順行・飛越し・逆行・証跡差・CAS競合を投入して受入する | 檸檬→Ｋの昇天→愛撫の順にaccepted、旧public不変、valid actual＋manifest commitは完成新版へ収束し、第三者変更は上書きしない |
| QT-F010-010 | [REQ-F010-014] | 自動 | F010 dataを既存pipelineへ投入しCatalog・sourceとroute集合を検査する | 9作者30作品、混線・固有分岐・参照欠損0となり、home・既存9作者route全件・梶井基次郎route・favorites・creditsのexact route集合だけを受理する |
| QT-F010-011 | [REQ-F010-015] | 自動 | 全作者routeを遷移・再読込・back/forwardし、音声・404を操作する | 初期open 0、同時再生1、切替停止、個別エラー隔離となる |
| QT-F010-012 | [REQ-F010-015] | 自動 | 全Catalog dialogueとcontrolの1対1、`aria-pressed`、自動再生0を照合する。既存/F010複数作者ID、重複・未知ID、破損JSON、未知version、件数/文字数上限、quota/write失敗、prototype/getter、CSS selector/path/URL hostile文字を投入し、favorite一覧からone-shot元作品移動後、直アクセス・reload・back/forwardも行う | exact plain JSON schemaとCatalog joinを通った安定IDだけを使用し、未知・重複・hostile値をDOM/path/URLへ渡さない。失敗時memory縮退、外部送信0、通常入口では初期全閉となる |
| QT-F010-013 | [REQ-F010-016] | 自動 | artwork provenance schemaのgenerator/version・規約snapshot・prompt・negative prompt・参照入力exact空配列・加工入力・全入力由来・author identity・原本/最終SHA・creditを検証する。PNGの形式、encoded/decoded bytes、幅、高さ、pixel、frame上限同値/+1、既存画像byte比較、dHash64-v1のbit順・lowercase 16桁hex・leading zeroを独立再計算し、Hamming距離`8/9`を投入する | 参照入力はexact `[]`、由来欠落・decode bomb・byte重複は0で、dHash距離8以下を拒否し9以上を近似扱いしないsealed独自画像だけを3作品書誌・creditsへjoinし、孤立creditが0件となる |
| QT-F010-014 | [REQ-F010-017] | 自動 | meta CSPの位置・個数・directive/value/order exact一致、directive欠落・緩和、危険DOM、外部自動request、悪性external anchor、Cookie/form/storage、secret検査、action権限を検査する。既存Playwright環境のF010必須IDのskip 0、clean checkoutのlocal Pages previewで画像200/WAV Range 206、公開route全件を確認する | 固定CSPより弱い状態、secret、権限違反、Critical/High、F010必須skip、外部自動通信が0となる。commit/dist/catalog digestと全route・全環境が一致する |
| QT-F010-015 | [REQ-F010-018] | 自動 | DOMAIN-F006.md確定済みの2022年公式XHTML上位500行と拡充CSV著者行のdigestをF010公開直前に再取得・再確認し、閲覧数合計・人物ID結合・同点・重複identity・追加上限+1を投入する | `targetAdditionalAuthors=7`、F010追加6/7、残1、最終上限10を再現し、人物別合計降順・同点人物ID数値昇順の入力/式/結果digest（`580164841c4ade8bc41862bc1ef295e92fba5b20f4b3907e04c95e972176b423`）が一致して重複0となる |
| QT-F010-016 | [REQ-F010-001][REQ-F010-002][REQ-F010-017] | 自動 | module-private policy mintとF010 approval snapshot commitに固定したqueue/SRS/QT/policyのcanonical path・bytes・SHAを再読込し、F009 baseline control、placeholder、偽policy brand、別definition/baseline descriptor、mutable workspace差替え、caller `approved=true`、symlink、junction、reparse、hardlink、case/Unicode衝突を投入する | F009 control、placeholder、forged brand・approval・baseline・path差替えを全て拒否し、内部pinからmintしたF010承認済みcontrol tupleだけがproduction brandへ昇格する |
| QT-F010-017 | [REQ-F010-017] | 自動 | workspace相対pathへabsolute、drive、UNC、device、`\\?\`、ADS、backslash、空/dot/dotdot、percent separator、control、Windows予約名、末尾dot/spaceを投入する。全handle検証完了後・rename実行直前のbarrierでparent junction/renameと置換target swapを競合させ、workspace外canaryを配置する | trusted root内のcanonical regular fileだけをancestor/target handle identity付きhandle-relative renameで処理し、network/device/root外アクセス、workspace外canary bytes変更、TOCTOU上書きが0となる |
| QT-F010-018 | [REQ-F010-017] | 自動 | release preparationへゲート①〜③と全証跡を投入し、ゲート④なしでprecheckを生成する。`recordPublished`へ偽造boolean、別releaseのapproval/deployment/smoke、API成功だがread-back trueを投入する。ゲート④後・deploy変数有効化前に直前baseline artifact availability/digestとrollback capabilityをpre-armし、hosted Pages smoke、rollback、disableまで継続する | preparationはゲート①〜③だけで成立する。`published`はゲート④、workflow/API/smoke/read-back falseをcanonical evidenceから再読込した同一tupleだけで成立する。失敗時は直前baselineのexact routeへ戻る |
| QT-F010-019 | [REQ-F010-019] | 自動 | QA-F010.md No.11の確定回答（実測で長大候補が検出された場合のみF007ローカル抽出層の分割ロジックを適用、共有音声モジュールは変更しない）に基づき、3作品の全候補speechText長を実測する。DOMAIN-F010.md §3.1実測（最大候補長133文字、Ｋの昇天）を根拠に、600文字閾値・1,335文字境界のいずれも超過しないことを確認し、分割ロジックが不発動のまま候補が不変であることを確認する。あわせて人為的に600文字超のfixture候補を投入した場合に既存F007ロジックが正しく分割・VOICEVOX synthesis実接続で成功することも回帰確認する | 3作品の実測候補はいずれも閾値以下で分割ロジックは不発動のまま候補が不変となる。人為fixtureでは既存分割ロジックが正しく機能し各piece長が閾値以下となる。共有汎用音声モジュール（`src/voice/generation.ts`・`src/voice/client.ts`）の差分は0件となる |

## 判定

- `QT-F010-001`〜`019`の全件PASSを必須とする。
- 手動・実機・目視・聴取は必須PASS証跡に含めない。
- 正式結果はexact release candidate tupleへ結合する。
- QT-F010-004・QT-F010-006はQA-F010.md No.6・No.5の回答確定（2026-08-24、いずれもA採用）を反映済み。
- QT-F010-019はQA-F010.md No.11（長大候補分割方式、A採用）とDOMAIN-F010.md §3.1実測（F010の実測最大候補長133文字、F007実測境界1,335文字を大きく下回る）を踏まえ、F009と異なりF010では機構不発動の確認を主眼としたfixtureを含む。
