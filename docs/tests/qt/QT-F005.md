---
feature: F005
title: 夏目漱石3作品追加 適格性試験仕様
status: Approved
version: 1.2.0
updated: 2026-08-08
---

# QT-F005 適格性試験仕様

| ID | 対応要求 | 自動化 | 手順 | 期待結果 |
|---|---|---|---|---|
| QT-F005-001 | [REQ-F005-001] | 自動 | 固定v0.4.0と統合後の既存projection・挙動をexact比較する | 3作者・12作品・674台詞・662音声、asset、初期全閉、お気に入りが一致する |
| QT-F005-002 | [REQ-F005-002][REQ-F005-003][REQ-F005-004] | 自動 | 候補からmanifestを生成し、重複・identity不一致fixtureを投入する | 一意の夏目漱石と3作品だけを受理する |
| QT-F005-003 | [REQ-F005-005][REQ-F005-006] | 自動 | 公式CSV・作者ページ・card・XHTMLと3規約を選定時とpredeployの独立snapshotで照合する。全A/AAAA・接続peer pin、IPv4-mapped/special address、redirect、encoded/decoded上限、status/content-type/charset、timeout、夢十夜だけの校正者欄欠落、他作品null、空文字、推測値、用途条件差分を検査する | 権利flagなし、著者、翻訳者0、公開中、新字新仮名、版、URL、raw bytes/path/hash/取得時刻/transport証跡がrelease tupleと一致する。全用途条件が`decision: allow`の場合だけ通し、差分・未判定・SSRF/decompression異常はnetwork/file漏洩なしで停止する。夢十夜だけcanonical nullを受理し、source/provenance/Catalog/loader/UI/creditsで「記載なし」となる。既存12作品のcanonical JSON bytesは不変となる |
| QT-F005-004 | [REQ-F005-007] | 自動 | 固定raw 161,913 bytes/Shift_JIS/SHAとexact XHTML 1.1 DOCTYPEを処理し、`div.notation_notes`の対象を検査する。DOCTYPE差替え・internal subset、file/httpのgeneral/parameter entity、billion laughs、XInclude、外部schema/stylesheet、深さ/node/属性/text上限+1を投入する | raw byte/SHA不変、許可contextの連続exact 2件だけが`<td>&#160;&#160;</td>`へ1対1変換され、processed SHAが固定値となる。exact DOCTYPE宣言はDTD取得・entity解決0で通り、他DOCTYPE等は外部network/filesystem access 0で拒否する。candidate ID・順序・表示文が決定的となる |
| QT-F005-005 | [REQ-F005-008] | 自動 | primary/secondaryへ別principal/session/runのauthorizationを発行し、same-principal、same-session、cross-work、replay、expiry、audience、prompt/template hash不一致、結果先見、premature adjudicator、seal改変を全候補へ適用する | 独立一致または不一致digest後に発行された第三裁定だけが確定し、tokenは一回限り、最終pending 0となる |
| QT-F005-006 | [REQ-F005-009][REQ-F005-010] | 自動 | 表示/TTS revisionと趣味の遺伝のCatalog notice・出典URL・hashを作者/作品/creditsへjoinし、欠落・改変fixtureを検査する | 原文、読み補正、抜粋・原作由来、公式表現注意が3配置でexact表示される |
| QT-F005-007 | [REQ-F005-011] | 自動 | approved/rejected混在で差分生成・再実行する | approvedだけを生成し、失敗・欠損・孤立0、同一音声を再利用する |
| QT-F005-008 | [REQ-F005-012] | 自動 | code point `500/501`、duration `120000/120001` ms、WAV `5760044/5760045` bytesと6容量区分の上限同値/+1を検査する。native guardへ偽live/free/time/hash、notice省略の一時巨大create→delete、Job breakaway、notice-only event、認可済みprocess generationのETW-only低レベルwrite/setinfo、buffer lossをそれぞれ投入する。開始時はSystem IO Provider、Microsoft-Windows-Kernel-Process、root実動probeを必須とし、ProcessStart欠落・QPC境界以前・ProcessSequenceNumber/任意event key不一致・probe timeout/未probe phase、同一PIDを別generationが再利用する両方向race、Job内認証済みchild正常終了後の遅延eventも投入する。stage後close→actual reportの同一work/candidateとexact 5 phase bindingも照合する | 上限同値を通し+1を停止する。guardが再計算したETW連番・file identity/length/hash・free bytesだけをactualへ使う。notice-only、偽造、QPC birth fence/sequence/probe/Job異常、loss時はjournalをcloseしない。最新start QPCより後でeventと保持handleのboot一意sequenceが完全一致し、保持handleが生存中ならJob所属し、signaledなら固定process identityが一致し、任意のevent keyが保持handle keyと一致し、active phase・既知eventを満たすETW-only低レベルeventはactualへ算入してcloseできる。生存中Job外、wait異常、Job照会失敗は拒否する。rootはsession開始前から保持する同一handleで認可する。notice省略の一時巨大create→deleteも容量から除外されない。別session/work/candidate、phase不足/余分/順序差のactualは拒否し、5 GiB未満または各停止境界では生成前に停止する |
| QT-F005-009 | [REQ-F005-002][REQ-F005-013] | 自動 | 1作品1sessionで3作品を、各sessionのQPC birth fence＋process sequence対応root probe→preview→ETW内stage→close後actual→metadata finalizeの順で受入し、全状態の順行・飛越し・逆行・証跡差・CAS競合と、prepared/artifacts-committed/capacity-measured/manifest-committed/closedの各phaseで停止・再開する。actual未生成・別work/journal/candidate・改ざん、probe未完了、nullable field先行設定、caller提供journal path/old-new SHAを偽造する | previewにactual循環がなく、root probe PASSかつ前作品finalize後だけ次sessionを開始し、valid closed actualを持つ完了作品だけが順序どおりacceptedとなる。旧public不変、内部導出したjournalと実体からactual未確定は旧版、valid actual＋manifest commitは完成新版へ収束し、第三者変更は上書きしない |
| QT-F005-010 | [REQ-F005-014] | 自動 | F005 dataを既存pipelineへ投入しCatalog・sourceとroute集合を検査する | 4作者・15作品、混線・固有分岐・参照欠損0となり、home・既存3作者・natsume-soseki・favorites・creditsのexact 7 routeだけを受理する |
| QT-F005-011 | [REQ-F005-015] | 自動 | 全作者routeを遷移・再読込・back/forwardし、音声・404を操作する | 初期open 0、同時再生1、切替停止、個別エラー隔離となる |
| QT-F005-012 | [REQ-F005-015] | 自動 | 全Catalog dialogueとcontrolの1対1、`aria-pressed`、自動再生0を照合する。既存/F005複数作者ID、重複・未知ID、破損JSON、未知version、件数/文字数上限、quota/write失敗、prototype/getter、CSS selector/path/URL hostile文字を投入し、favorite一覧からone-shot元作品移動後、直アクセス・reload・back/forwardも行う | exact plain JSON schemaとCatalog joinを通った安定IDだけを使用し、未知・重複・hostile値をDOM/path/URLへ渡さない。失敗時memory縮退、外部送信0、利用者移動時だけ対象panelを展開し、通常入口では初期全閉となる |
| QT-F005-013 | [REQ-F005-016] | 自動 | `ArtworkProvenanceV4`のgenerator/version・規約snapshot・prompt・negative prompt・参照入力exact空配列・加工入力・全入力由来・author identity・原本/最終SHA・creditを検証する。PNGの形式、encoded/decoded bytes、幅、高さ、pixel、frame上限同値/+1、既存画像byte比較、dHash64-v1のbit順・lowercase 16桁hex・leading zeroを独立再計算し、Hamming距離`8/9`を投入する | 参照入力はexact `[]`、由来欠落・decode bomb・byte重複は0で、dHash距離8以下を拒否し9以上を近似扱いしないsealed独自画像だけを3作品書誌・creditsへjoinし、孤立creditが0件となる |
| QT-F005-014 | [REQ-F005-017] | 自動 | meta CSPの位置・個数・directive/value/order exact一致、directive欠落・緩和、危険DOM、外部自動request、悪性external anchor、`noopener noreferrer`またはglobal `no-referrer` meta欠落、Cookie/form/storage、tracked/untracked/dist/upload/workflow log/source mapのsecret、action 40桁pin・最小権限・`persist-credentials: false`、auditを検査する。既存6 Playwright projectでF005必須IDのskip 0、意図的asset skip allowlist、clean checkoutのlocal Pages previewで画像200/WAV Range 206、公開7 routeを確認する | 固定CSPより弱い状態、secret、権限違反、Critical/High、F005必須skip、外部自動通信が0となる。既存purpose別HTTPS allowlistのexternal anchorだけを安全属性付きで維持し、commit/dist/catalog digestと6環境・全routeが一致する |
| QT-F005-015 | [REQ-F005-018] | 自動 | 2022年公式XHTML上位500行と拡充CSV著者行をfixture化し、閲覧数合計・人物ID結合・同点・重複identity・追加上限+1を投入する | `targetAdditionalAuthors=10`、F005追加1/10、残9、最終上限13を再現し、人物別合計降順・同点人物ID数値昇順の入力/式/結果digestが一致して重複0となる |
| QT-F005-016 | [REQ-F005-001][REQ-F005-002][REQ-F005-017] | 自動 | module-private policy mintとF005 approval snapshot commitに固定したqueue/SRS/QT/CHG/policyのcanonical path・bytes・SHAを再読込し、F004 baseline control `fc1aa7bc6cf91f8b4bac20ba5d147921bf081a6d`、placeholder、偽policy brand、別definition/baseline descriptor、mutable workspace差替え、caller `approved=true`、all-args swap、symlink、junction、reparse、hardlink、case/Unicode衝突を投入する | F004 control、placeholder、forged brand・approval・baseline・path差替えを全て拒否し、内部pinからmintしたF005承認済みcontrol tupleだけがproduction brandへ昇格する |
| QT-F005-017 | [REQ-F005-017] | 自動 | workspace相対pathへabsolute、drive、UNC、device、`\\?\`、ADS、backslash、空/dot/dotdot、percent separator、control、Windows予約名、末尾dot/spaceを投入する。全handle検証完了後・rename実行直前のbarrierでparent junction/renameと置換target swapを競合させ、workspace外canaryを配置する | trusted root内のcanonical regular fileだけをancestor/target handle identity付きhandle-relative renameで処理し、network/device/root外アクセス、workspace外canary bytes変更、TOCTOU上書きが0となる |
| QT-F005-018 | [REQ-F005-010][REQ-F005-017] | 自動 | F005のdata-driven noticeをtext/sourceUrl/sourceRawSha256/textSha256/placementsで3配置へ投影し、HTML payload、配置欠落、source差、悪性URL/anchor属性欠落を投入する。同時にF003 noticeのcanonical projectionを比較する | F005 noticeは公式snapshot由来のtext nodeとpurpose別allowlist済み安全anchorとしてexact 3配置され、F003のschema・文言・bytesは不変となる |
| QT-F005-019 | [REQ-F005-017] | 自動 | release preparationへゲート①〜③と全証跡を投入し、ゲート④なしでprecheckを生成する。`recordPublished`へ偽造boolean、別releaseのapproval/deployment/smoke、API成功だがread-back trueを投入する | preparationはゲート①〜③だけで成立する。`published`はゲート④、workflow/API/smoke/read-back falseをcanonical evidenceから再読込した同一tupleだけで成立する |
| QT-F005-020 | [REQ-F005-001][REQ-F005-017] | 自動 | ゲート④後・deploy変数有効化前にv0.4.0 artifact availability/digestとrollback capabilityをpre-armし、別pending journal差替えを拒否する。その同一staged testをdeploy、hosted Pages smoke、rollback、disableまで継続し、各phaseを中断する。hosted artifact/commit/dist/catalog digest、画像200、WAV Range 206、公開7 routeを検査し、主process停止後に別process watchdogを起動する | pre-arm PASS証跡なしではdeployを開始しない。hosted tupleと全routeが一致した場合だけ成功し、失敗時はv0.4.0 exact 6 routeへ戻る。全経路でdeploy変数read-back falseかつjournal closedとなるまで`published`にしない |

## QT-F005-008/009補足: CHG-F005-052 T-110 hosted影響判定

T-110の限定再結合は、同一source commitでpinned native buildとkernel ETW preflightがPASSし、production assemblyを直接参照するtarget suite `CHG-F005-036/T-110`のcase manifest SHA・期待件数・PASS件数が一致した場合にhosted影響PASSとできる。suiteはall-true、全単独false、QPC境界、実directory/file identity、private Job childのalive/signaled/別generation/Job外、production共有inspect処理のthrowによる`INITIAL_TUPLE_INSPECTION_FAILED`を含む初回・再検査19固定code、production callbackの共有規則呼出順を決定的に検査する。throw fixtureはtest assembly内だけに閉じ、production IPC・switch・環境変数へ公開しない。

canonical evidenceのProgram/project/build.ps1/global.json/workflow/test project/test source/pinned binary/実load production assembly/test assembly/executable/case manifest SHAとMVID、ImageOS/ImageVersion・Windows build/UBR・PowerShell version、raw値非公開、semantic state不変、`public/`・`data/`差分0、candidate branch不存在、同一source SHAのPages run ID/attempt/conclusionとdeploy job conclusion `skipped`をexact照合する。build/testを行うfeature verificationはworkflow全体`actions: read`/`contents: read`で、credential、push、artifact uploadを持たない。target stdout/stderrはRUNNER_TEMPへ上限付きredirectし、同じread-only jobがPages run terminalとdeploy `skipped`をbounded pollする。test-only production入口、条件付き認可、未知・重複・欠落marker、case不足/余分、SHA/MVID差、Pages相関timeout/多重/欠落があればFAILとする。この判定はQT-F005-008/009のT-110影響範囲だけを閉じ、通常production pipelineの容量actual、3作品accepted、公開判定を代替しない。

## QT-F005-008/009補足: CHG-F005-055 T-109 hosted影響判定

T-109は、同一source commitでpinned native buildとkernel ETW preflightがPASSし、production stageとtargetが共有するproduction-owned evaluatorへ実Windows fixtureを投入した固定manifestの期待件数・PASS件数・SHAが一致した場合だけhosted影響PASSとできる。deferred 0/1/複数、event/deferred QPC全境界、完全tuple各単独不一致、current identity、alive/signaled/世代差/Job外/identity差替え、14 stage、先行code優先を排他的に検査する。caller供給actual、偽identity・偽process結果、test-only production入口、認可・lease binding・capacity・semantic state変更、別認可fall-throughがあればFAILとする。

T-110/T-122/T-112/T-109 targetと通常native suiteが無縮退し、`schemaVersion`・`kind`・top-level `result`を含むcanonical evidence exact keys、実load assembly SHA/MVID、target source/selector/manifest SHA、raw値非公開、`public/`・`data/`差分0、candidate branch不存在、同一source SHAのPages deploy `skipped`を満たすことを確認する。この判定はT-109影響範囲だけを閉じ、通常production pipeline、capacity actual、候補受入、公開判定を代替しない。

QT-F005-008の容量境界試験には、Job継承した固定native `--write-through-once` helperによるWAV temporaryの`CREATE_NEW | FILE_FLAG_WRITE_THROUGH | FILE_FLAG_DELETE_ON_CLOSE`、同一handle write/flush/readback/SHA、同一handle rename、commit/abort/EOF/応答前killを追加する。正常系は書込み前にexact path・phase・Job producerを予約し、先着System SetInfoのidentity・容量・空き容量・ETW連番を封印して、後着する同一FileObject・path・process世代・identity・正しいQPC順序のJob Createでだけ保留解除・観測再投入する。replacement、Cleanup後pointer再利用、FileObject/path/phase/sequence/QPC不一致、未予約、System write/create/rename/delete、未解決保留、helper終了前completeは不合格とする。create notice後もdelete-on-closeを解除せずrenameし、rename notice認証後だけ`FileDispositionInfoEx(ON_CLOSE)`で解除する。kill・crash・EOFではtemporary名またはrename後名の同一native identityだけが消え、同名replacement、第三者entry、置換staging rootは保持されること、pathname-only再帰削除が0件であることを実Windowsで確認する。

同じQT-F005-008で、rename syscall前にexact targetを予約し、予約後QPCのtarget System SetInfoを結合済みFileObject・identityへ相関する。予約前event、別target、target既存、二重予約、別FileObject/identity、未消費予約でのcompleteを不合格とし、認証済みrename通知後だけtarget予約QPCが新current下限へ昇格され、通知後に遅延到着した予約前eventも拒否されることを実Windowsで確認する。

hosted実Windowsではpinned native executableをRUNNER_TEMPのGUID付き新規directoryへcopy後SHA照合して全native経路から起動し、hello self hashが固定pinへ一致すること、helper startupのtooling metadataがworkspace artifact監視へ混入しないこと、workspace内WAV mutationは全件ETW算入されることを確認する。workspace内、relative、SHA不一致、hardlink、junction overrideは候補保存前に停止する。

同じhosted試験で全native tooling processのcwdをexternal executable parentへ固定し、hello cwd一致booleanを確認する。write helper spawnからwrite予約までのworkspace tooling mutationは0件とし、artifact build childとcandidate/audio/stagingのworkspace mutationは従来どおりETWへ算入する。

unknown System SetInfoで停止する場合、hosted公開診断は固定top-level/extension/実体/lease bucketだけを含み、path・basename・host固有値を含まない。診断値を根拠に認可を自動緩和せず、candidate保存前の安全停止を維持する。

予約済みwrite path認識後に相関不一致で停止する場合、hosted公開診断は固定12段階の相関stageだけを含み、path・FileObject・identity・sequence・QPC・host固有値を含まない。診断値を根拠に認可を自動緩和せず、candidate保存前の安全停止を維持する。

leaseなしCACHE WAVで停止する場合、hosted公開診断は同一phaseで完了したwriteとの`DONE_ID/DONE_CHANGED/DONE_MISSING`関係だけを追加し、path・identity・host固有値を含まない。診断値を根拠に認可を自動緩和せず、candidate保存前の安全停止を維持する。

`DONE_ID`をhostedで確認した後の再試験では、同一phase/path/identity、予約後かつwrite完了以下のQPC、互換FileObject binding、System SetInfoの完全一致eventだけが元worker世代へ再結合され、次のWAVまたは後続phaseへ進むことを確認する。差替え・欠落・別tupleは従来どおり候補保存前に停止する。

active leaseがclosedの時点で別pathの完了write遅延eventが到着しても、そのactive leaseの`LEASE_CLOSED`へ誤分類せず、完了writeの完全tupleで再結合されることをhostedで確認する。同一active pathのclosed eventは停止を維持する。

完了write再結合条件で停止する場合、hosted公開診断は固定10 stageだけを含み、path、identity、FileObject、PID、sequence、QPC、host固有値を含まない。診断値を根拠に認可を自動緩和せず、次の変更管理で個別に影響判定する。

`AFTER_COMPLETION`で停止する場合は完了後100ms／500ms／2秒／10秒の固定bucketだけを公開し、raw時間差を含めない。bucket取得だけでは認可を緩和せず、別変更で安全境界を決定する。

hostedで`WITHIN_500MS`を確認した候補は、他の完全tuple条件を維持した500ms上限で再試験する。処理継続時も容量actual、候補identity、公開tree不変を確認し、上限超過時は安全停止する。

500ms上限の再試験で`WITHIN_2S`を確認した場合は、hosted実測済み2秒を上限として再試験する。2秒超は停止し、処理継続時も容量actual、candidate identity、公開tree不変を確認する。

2秒再結合通過後にactive lease closedで停止する場合は、snapshot/binding/current/identityの固定stageだけを公開する。`CANDIDATE`でも自動認可せず、次の変更管理で安全条件を判断する。

hostedで`CANDIDATE`を確認したclosed leaseは、同じ完全tupleとidentity二重照合の下だけ再結合する。処理継続時も容量actual、candidate identity、公開tree不変を確認し、他stageは停止する。

closed lease再結合通過後にSystem未結合write既知pathで停止する場合、hosted公開診断はFileObject、active lease snapshot/current/identity、open/closed、完了write関係を表す固定10 stageだけを含む。診断だけでは認可を変更せず、候補保存・公開tree変更は0件を維持する。

`OTHER_KNOWN_PATH`を確認した場合は、top-level・拡張子・実体種別・lease状態の固定bucketだけを公開する。bucket取得だけでは代理認可せず、候補保存・公開tree変更0件を維持する。

`CACHE_OTHER_DIRECTORY_NO_LEASE`を確認した場合は、snapshot/current identity・同一phase root owner・root activeの固定stageだけを公開する。`CANDIDATE`でも自動認可せず、候補保存・公開tree変更0件を維持する。

hostedで`SYSTEM_DIRECTORY_WRITE_REJOIN_CANDIDATE`を確認したcache directory writeは、同じ完全tupleとidentity二重照合の下だけroot worker世代へ再結合する。容量actual、candidate identity、公開tree不変を維持する。

leaseなしdirectory write再結合後に`CACHE_OTHER_DIRECTORY_UNBOUND_LEASE`で停止する場合、directory候補を先行判定し、active leaseの存在・同一phase・親directory一致・未結合・open・Job escapeを表す固定13 stageだけを公開する。`CANDIDATE`でも自動認可せず、次の変更管理で安全境界を決定し、候補保存・公開tree変更0件を維持する。

同じdirectory writeがtiming差により`CACHE_OTHER_DIRECTORY_BOUND_LEASE`となる場合も、同じ固定13 stage診断へ接続して`LEASE_BOUND`で停止する。boundを根拠に認可を自動緩和せず、候補保存・公開tree変更0件を維持する。

active lease中directory診断通過後に`SYSTEM_PROCESS_BOUND_FILE_OBJECT`で停止する場合、snapshot/path/current identityとactive leaseのphase/path/FileObject binding/closed/Job状態を表す固定11 stageだけを公開する。`CANDIDATE`でも自動認可せず、候補保存・公開tree変更0件を維持する。

bound active lease directory writeで`LEASE_BOUND`まで確認した場合は、lease snapshot、FileObject bindingのpath/identity、filesystem current identity、Job状態を追加した固定18 stageだけを公開する。`CANDIDATE`でも自動認可せず、候補保存・公開tree変更0件を維持する。

rename後に旧lease pathが消えた場合は、予約済みtarget・同一親directory・予約QPC後・target identity・Job状態の固定8 stageだけを公開する。`CANDIDATE`でも自動認可しない。

rename予約前eventを確認した場合は、lease current-path予約以前を`BEFORE_LEASE_RESERVATION`、lease予約後からrename予約前を`AFTER_LEASE_RESERVATION`として固定分類する。raw QPCを公開せず、どちらのbucketでも自動認可、容量actual確定、候補保存、公開tree変更を行わない。

System結合済みFileObjectがrename target pathで観測され、active lease current pathと不一致の場合は、pending target、予約順序、QPC三境界、旧current path欠落、snapshot path/identity、binding、closed、Job状態の固定14 stageだけを公開する。`CANDIDATE`でも自動認可、容量actual確定、候補保存、公開tree変更を行わない。

`AFTER_LEASE_RESERVATION`のdirectory writeは、既知directory・active lease・rename target・QPC・保持process identity・Job/遅延eventの完全tuple一致時だけ元のlease worker世代へ再結合する。認可直後の4点再照合を必須とし、不一致や取得異常は固定codeだけを公開して停止する。容量actualは既存ETW observationから再計算し、raw値非公開、candidate・`public/`・`data/`差分0件を維持する。

pending rename targetなしでactive leaseと異なる既知bound FileObject pathを観測した場合は、path種別、directory/root、leaseの状態を評価順どおり固定19 suffixへ分類する。`NO_PENDING_CANDIDATE`を含む全stageで認可・容量actual確定・候補保存を行わず、83〜101文字の固定codeだけを公開し、raw値と`public/`・`data/`差分を0件に保つ。

その分岐でlease FileObjectが未結合の場合は、予約QPC、current、deferred完全tuple、保持process世代とJob状態を固定順で評価し、`NO_PENDING_UNBOUND_*`14 codeだけを公開する。signaledと`UNBOUND_CANDIDATE`を含む全stageで認可・binding・容量actual確定・候補保存を行わず、raw値非公開と`public/`・`data/`差分0件を維持する。

bound lease directoryの既存`CANDIDATE`は、cheap predicate、完全tuple、保持processの初回inspectionがすべて一致する場合だけlease保存producer世代へ再結合する。認可後の6項目再照合はproduction共有の純粋ruleでexact codeと先行失敗優先を決定し、record直前process再inspectionを必須とする。初回7＋再照合12の固定code以外を公開しない。容量actualは既存ETW observationから算出し、raw値非公開、binding・candidate・`public/`・`data/`条件を維持する。

write completionはrename成立後にprivate prepareでroot owner identityを含むevent-time tupleを封印し、非重複epoch、bounded未適用queue、QPC deadline、relevant/accounted fence、seal/保持handle/native identity/checked容量の再検査、ETW順replay後の再fenceを完了した場合だけleaseを完了する。final排他admission中にcounter/queueが安定した場合だけseal/lease/phase stateを更新し、競合時はstate無変更で再試行する。callback配送順にかかわらず同じactualまたは同じexact 21 codeとなり、生のpath/QPC/identity/FileObject/process handleを公開せず、既存notice/candidate/公開条件を維持する。

completion drain replayでは、初回prepareをcallback admission writerで線形化したimmutable baseline、通常callbackとCleanupの共通proofSequence、FileObject exact generation、current/directory/other-bound retained handleを正本とする。pathless Cleanupは同じFileObject数値の再利用後に旧・新generationを識別できない場合、新generationと全semantic stateを変更せず`BINDING_MISMATCH`へ停止する。rename後pathの再openとsemantic mapの現在状態を認可根拠にせず、batch全件をshadow stateで検証してから一括適用し、後半失敗でも容量・Observation・noticeを部分更新しない。raw proof/handle値を公開せず、同一source SHAの3 attemptでは既知production pipelineが全件成功して同じcandidate内容となることを必須とする。

generic late eventがcompleted-retained parent、別active lease、same parent、現在予約QPC後の全条件を満たす場合だけwrite/setinfoの2固定codeへ細分化する。認可とstateを変更せず、その他はgeneric LATEを維持する。外部23 code以外と生のpath/QPC/FileObject/identity/process値を公開しない。

generic lateの複合条件不一致は、event kind別にseal state、current path、active lease欠落、active parent不一致、active予約QPC同値以前の最初軸または`MIXED_CAUSES`の12固定codeへ分類する。完全一致は既存2 codeを最優先する。同一lease不変条件違反はgeneric LATEでfail-closeする。認可とstateを変更せず、外部35 code以外と生値を公開しない。

単一exact late `SETINFO_CURRENT_PATH`は、completed recordとsealの不変tupleが一致する場合だけ、既存completed-write System SetInfo認可へ引き渡す。System PID、phase、予約QPC、2秒window、binding、current existence、identityの全条件を維持し、拒否後は他認可へfall-throughしない。認可PASS後はhandoff sealを持たないnormal callbackとして既存`OtherBound` proofを適用し、handoff sealのsealed proof/recheck/replayやseal `EventCount`を変更しない。その他のcompletion drain、外部35 code、生値非公開条件は変更しない。

単一exact late SetInfoが`CompletionRequested` sealのcurrent FileObject exact generationで、completion QPCより後かつdrain deadline以下の場合は、明示post-request marker付きsealed replayだけを許可する。SealedCurrent proof、retained current identity、producer PID/start key/sequence、process signaled、atomic replay、final fenceを全て維持する。write、parent、別generation、deadline超過、flagなしは停止し、外部35 code、生値非公開、candidate/Pages条件を変更しない。

exact late `WRITE_AT_OR_BEFORE_ACTIVE_RESERVATION`は、active leaseのimmutable registered producer birth recordによりrecord欠落、tuple不一致、event birth同値以前、event birth後の4固定codeへ分類する。可変PID birth mapを使わず、認可・stateは変更しない。外部39 code以外と生のpath/QPC/FileObject/identity/process値を公開せず、candidate/Pages条件を維持する。

単一exact late post-reservation parent writeは、別active lease・同一parent・同一phase・予約後・unbound FileObjectを再確認してから、既存active directory認可だけへ引き渡す。引き渡し先の全tuple/process/recheckをOtherBound proof identityとともにcapacity適用前に実行し、拒否後は他認可へ流さない。旧sealのproof/replay/EventCount、外部39 code、生値非公開、candidate/Pages条件を変更しない。

exact late `WRITE_ACTIVE_PRODUCER_RECORD_MISSING`は、active lease予約handlerがgate内で観測済みのimmutable producer birth snapshotにより、4固定codeへ分類する。snapshotのinitial reservationはbirth tupleへ、renameで昇格し得るcurrent path reservationはevent上限へ使用する。`current >= initial`かつ`event <= current`を満たすrename後の`initial < event <= current`はbirth後code、current逆転またはcurrent超過はSTATE_CHANGEDで停止する。予約以前QPCでもgate待ちのProcessStartは未観測missingとして認可しない。既存registered record経路を維持し、予約後の可変birth mapを使わず、認可・stateは変更しない。外部43 code以外と生のpath/QPC/FileObject/identity/process値を公開せず、candidate/Pages条件を維持する。

write予約はproducer保持handleと同一sequenceのProcessStart recordを最大10秒、gateを解放する`Monitor.Wait`で待つ。record観測後にphase/path/pending lease、process tuple/non-signaled/Job、birth tupleを再検査し、待機後に採取した予約QPCとの`phase < birth <= reservation`一致時だけleaseを公開する。timeout、tuple、process identity、state changedは固定codeで予約前停止し、認可、completion-drain 43 code、IPC keys、candidate/Pages条件を変更しない。

Dispose/cancelはgate内abort/Pulse、pipe waiter完了、resource破棄の順を守る。QPC deadlineはchecked生成、ceiling wait、wake後再QPCで判定し、stale birth fingerprintはmatching updateを待つ。既存first failureを上書きせず、予約fence4 codeだけを`F005_`付きで外部伝播する。予約transaction失敗時はlease等を未作成とし、wait中の正当な並行ETW更新は保持する。

単一exact late `WRITE_ACTIVE_LEASE_MISSING`は、CompletedRetained parent、same active voice phase、pending lease null、System/BIRTH_MISSING、unbound FileObject、completion後を満たす場合だけ既存no-lease root directory完全認可へhandoffする。PASS後も旧seal replayを使わず、OtherBound proofとimmutable contextでphase/no lease/seal/root/directoryをadmit前およびcapacity前に再検査する。queued drift失敗はsemantic stateを未変更に保ち、bounded内部証拠をpoison後再適用せずDisposeで解放する。他認可へfall-throughせず、新外部code、候補保存、Pages公開を追加しない。

generic `EVENT_TUPLE_MISMATCH`はlookupのepoch-empty-no-late-proof/exact missing/exact ambiguousとsealed recheckのseal missing/seal ambiguous/fields mismatchへ固定分離する。診断は候補・proof・認可・semantic stateを変更せず、6 codeを追加したcompletion-drain 49 codeを全層で共有する。予約fence4 code、IPC、candidate保存、Pages公開を変更しない。

aggregate `WRITE_ACTIVE_LEASE_MISSING`は、distinct late candidateが2件以上の場合だけ生値なしの`COMPLETED_NO_LEASE_DIRECTORY_HANDOFF_CANDIDATE_AMBIGUOUS`へ分類する。exact 1は上流保証済みpredicateと既存handoff/known-directory完全認可を変更せず、count 0/負値/別aggregateは既存codeでfail-closeする。snapshotから分類/throwまたは既存handoffまでを同一gateで線形化し、ambiguous後は他認可へfall-throughせずsemantic stateを変更しない。candidate count/path/QPC/PID/FileObject/identity/sequence/handleを外部へ出さず、completion-drain 50 codeと予約fence4 codeを全層同期し、candidate、Pages、`public/`、`data/`条件を変更しない。

aggregate `LATE_RETAINED_PARENT_OTHER_ACTIVE_SAME_PARENT_POST_RESERVATION_WRITE`は、distinct late candidateが2件以上の場合だけ生値なしの`ACTIVE_DIRECTORY_HANDOFF_CANDIDATE_AMBIGUOUS`へ分類する。既存aggregateがexact post-reservation candidateを他causeより優先するため、exact 2件とexact 1件＋別causeの両方を順序非依存で対象とする。count -1/0/1または別aggregateはoverrideせず、count 1のCHG-F005-044既存active-directory handoff predicateを変更せず評価するが通過を保証しない。snapshot→aggregate→cardinality→throwまたは既存分岐を同一gateで線形化し、再列挙・mutable rereadを行わない。ambiguous後はcompleted/no-lease/post-request/active-producer/known-directory/proof/capacity/applyへfall-throughせずsemantic stateを変更しない。count/候補内容・順序/path/QPC/PID/FileObject/identity/sequence/handleを全surfaceへ出さず、completion-drain 51 codeと予約fence4 codeを全層同期し、candidate、Pages、`public/`、`data/`条件を変更しない。

active-directory候補多重時は、CHG-F005-044既存handoff predicateから総candidate数条件だけを除いた共有predicateを各候補へ適用し、適格数をEXACT_ONE/AMBIGUOUSの2固定codeへ分類し、正常構成不能な0件はSTATE_CHANGEDへ閉じる。count 1の既存認可結果は維持し、多重時はEXACT_ONEでも認可しない。completed-no-leaseは既存多重codeを維持する。snapshotから固定code throwまで同一gate、順序非依存、semantic state不変、全後段非fall-throughとする。total/eligible count、predicate bitset、candidate/path/QPC/process/identity値を公開せず、completion-drain 53 codeと予約fence4 codeを全層同期し、candidate、Pages、`public/`、`data/`条件を変更しない。

## 判定

- `QT-F005-001`〜`020`の全件PASSを必須とする。
- 手動・実機・目視・聴取は必須PASS証跡に含めない。
- 正式結果はexact release candidate tupleへ結合する。
