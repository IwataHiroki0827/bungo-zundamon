---
id: CHG-F005-029
feature: F005
level: design
status: done
date: 2026-07-30
---

# CHG-F005-029 bound active lease directory writeの完全候補固定診断

## 変更理由

GitHub Actions run `30523793166`は`audio-renamed`まで進み、`F005_ETW_PID_NOT_JOB_MEMBER_SYSTEM_DIRECTORY_ACTIVE_LEASE_WRITE_REJOIN_LEASE_BOUND`で安全停止した。cache directoryのsnapshot/current identity・root ownerと、同一phase・親pathのactive lease結合までは確認できたが、lease自身のsnapshot、FileObject binding、current identity、Job状態は未確認である。

## 変更内容

- exact `CACHE_OTHER_DIRECTORY_BOUND_LEASE`を、directory候補6 stageとleaseの存在・phase・親path・closed・bound・snapshot・binding存在/一致・current/identity・Job escapeを含む固定18 stageへ細分化する。
- directory候補後もlease→phase→親path→closed→bound→snapshot→binding→current→identity→Jobの段階returnを維持する。
- bindingは`filesByObject[lease.FileObject]`のpath/identityがlease path/snapshot identityへ完全一致する場合だけ成立する。
- `CANDIDATE`を含め認可、容量actual、候補保存、公開条件は変更しない。

## セキュリティ境界

System PID 0/4、write、非0未結合の観測FileObject、既知cache directory、active bound lease bucketを前提とする。directoryとchild leaseの双方を同一phase・native identityへ結合し、生path・identity・FileObject・PID・sequence・QPCは出力しない。未知stage/suffixは固定`DIRECTORY_UNKNOWN`またはgeneric/nullへ閉じる。

CHG-F005-029反映後のnative pinはProgram SHA-256 `11c52de92c12b21292fd87f918232f1b67e8030e3fd994341f5f82c9eac637db`、output binary SHA-256 `3d9e1b91ce09b9c762c84cb03307d55127a19037ecfd795db48fab33ebbc7b6b`、75,032,412 bytesとする。

## 検証

- 固定18 stage、directory/lease段階評価、binding/current identity、Job評価1回、未知suffix拒否、runner exact伝播、認可分岐不変を検査する。
- native規則165件、対象回帰124件、型、ESLint、同一SHA再現build 2回、trace、差分検査、公開差分0をPASSした。
- 独立レビューはHigh/Medium/Low 0件であり、commit固定のhosted productionを再試験する。
- Actions run `30524741788`で`LEASE_CURRENT_MISSING`を取得したため、rename先診断をCHG-F005-030へ分離する。

## 承認

認可を変更しない診断追加であり、固定stage取得を完了したため`done`とする。
