---
id: CHG-F005-035
feature: F005
level: design
status: in-review
date: 2026-08-06
---

# CHG-F005-035 pendingなしbound directory writeとunbound leaseの固定診断

## 変更理由

commit `e3f85b32b9dfe14b6b219c5471ff1ad4170de358`のActions run `31035627381`は、attempt 1で既知のdirectory lease binding不一致へ安全停止し、attempt 2でCHG-F005-034の新stage `F005_ETW_PID_NOT_JOB_MEMBER_SYSTEM_PROCESS_BOUND_FILE_OBJECT_REJOIN_NO_PENDING_LEASE_UNBOUND`へ到達した。観測pathは既知bound directoryの完全候補まで進んだ一方、active leaseはFileObject未結合であり、lease予約・current path・deferred correlation・process状態との関係が未確定である。

## 予定変更

既存の`REJOIN_NO_PENDING_LEASE_UNBOUND`失敗だけを対象に、認可を変更せず次の固定stageへ細分化する。

1. lease snapshotが既に存在する不変条件違反を`UNBOUND_SNAPSHOT_PRESENT`へ固定する。
2. event QPCがlease `CurrentPathReservedAtQpc`以下なら`UNBOUND_BEFORE_RESERVATION`へ固定する。eventが予約QPCと同値なら拒否し、予約QPC + 1 tickだけを後段候補にする。
3. lease current pathの再open取得に失敗したら`UNBOUND_CURRENT_INSPECTION_FAILED`、欠落なら`UNBOUND_CURRENT_MISSING`へ固定する。
4. deferred System SetInfoが0件なら`UNBOUND_DEFERRED_MISSING`、exact 1件でないか、次のtupleのどれかが不一致なら`UNBOUND_DEFERRED_TUPLE`へ固定する。
   - `WorkerPid`と`ProducerSequenceNumber`がlease保存値と一致する。
   - `Phase`と`WorkId`がactive phaseと一致し、`PhaseInstanceId`がleaseとactive phaseの双方に一致する。
   - `RelativePath`とsnapshot pathがlease current pathと一致する。
   - FileObjectが非zeroで、現在の`filesByObject`に未結合である。
   - `CurrentPathReservedAtQpc < deferred QPC <= event QPC`である。deferred QPCの予約同値は拒否、予約 + 1 tickとevent同値は候補、event + 1 tickは拒否する。
5. 再openしたlease current identityがdeferred snapshot identityと不一致なら`UNBOUND_CURRENT_ID_MISMATCH`へ固定する。
6. 保持process handleのinspectionはidentity取得、wait、alive時だけJob照会の順に行い、wait、identity、Job照会失敗をそれぞれ`UNBOUND_PROCESS_WAIT_FAILED`、`UNBOUND_PROCESS_IDENTITY_FAILED`、`UNBOUND_JOB_QUERY_FAILED`へ固定する。取得PID/start key/sequenceがlease保存値と不一致なら`UNBOUND_PROCESS_TUPLE`へ固定する。
7. 同一世代processがsignaledならJob照会せず`UNBOUND_PROCESS_SIGNALED`、aliveかつJob外なら`UNBOUND_LEASE_ESCAPE`、aliveかつJob memberの全件一致だけを`UNBOUND_CANDIDATE`へ固定する。

上記14 suffixをexact集合とし、既存bound FileObject外側tuple、directory/root候補、lease phase/parent/open、FileObject未結合、pending targetなしの評価順を維持する。full code長は96〜112文字で127文字上限を満たす。旧exact `...NO_PENDING_LEASE_UNBOUND`、未知suffix、`UNBOUND_CANDIDATE`を含む全stageをfail-closedとし、自動認可、lease binding、容量actual確定、候補保存、公開tree変更へ接続しない。raw path、QPC、identity、FileObject値は公開しない。

## 影響範囲

`tm.json`とFUN-F005-047の容量guard経路から、`REQ-F005-012`、`REQ-F005-013`、`DES-F005-006`、`FUN-F005-047`、`UT-F005-047`、`IT-F005-005`、`QT-F005-008`を影響対象とする。

## 承認

既承認の容量guard内で認可を変更しないdesign診断追加であり、要求承認ゲートは設けない。影響文書更新、独立レビュー、影響試験、hosted再試験まで`in-review`とする。

計画レビューは初回High 0 / Medium 2 / Low 0を受け、deferred完全tuple、current再照合、QPC境界、process inspection順を追記した。再レビューはHigh 0 / Medium 0 / Low 0でPASSした。

## 試験方針

- native規則試験で上記14 suffix、eventの予約同値/予約+1、deferredの予約同値/予約+1/event同値/event+1、deferred 0/1/複数・全tuple、current取得失敗/欠落/identity不一致、process inspection 3異常・世代不一致・signaled/Job escape、`CANDIDATE`までの評価順と排他性を固定する。
- 構造試験で保持process inspectionがidentity→wait→alive時Jobの順で、signaled時はJob非照会、alive Job memberだけが`CANDIDATE`になることを確認する。
- TypeScript exact union/anchored regex/runnerで全固定code、127文字上限、旧exact・未知suffixのgeneric化、生値非公開を確認する。
- 構造試験で既存`NO_PENDING_LEASE_UNBOUND`分岐だけが新診断へ入り、認可helper、lease binding、容量actual、candidate・`public/`・`data/`を変更しないことを確認する。
- DD-F005、UT-F005、IT-F005、QT-F005、本CHG、Program/binary/bytes pinとbuild evidenceを同期し、同一SHA再現buildを2回行う。
- commit固定hosted試験で新固定stageを取得し、candidate branch未作成、Pages deploy skip、`public/`・`data/`差分0を確認する。

## 実装結果

旧`NO_PENDING_LEASE_UNBOUND`分岐だけを`NO_PENDING_UNBOUND_*`のexact 14 stageへ接続した。event/deferred QPC、lease current再open、deferred完全tupleとcurrent identity、保持processのidentity→wait→alive時Job照会を固定順で評価し、signaled・Job escape・`CANDIDATE`を含む全stageをfail-closedとした。認可helper、lease binding、容量actual、候補保存・公開条件は変更していない。

native規則試験は261件、関連Vitestは122件、型、ESLint、同一SHA再現build 2回をPASSした。Program SHA-256は`816c77dd1ff28498d271273d69f40e0aa5dd6d9585d48bc74c0f58ad0b18837d`、binary SHA-256は`903b4272f3daa3ae44c2e7ec82d046a62249de594d6745630f25ddeb20671c71`、75,048,796 bytesである。独立受入はHigh 0 / Medium 0 / Low 0でPASSし、`public/`・`data/`差分は0件である。hosted再試験を後続で実施する。

## 実装結果

既存のbound FileObject、directory/root、lease phase/parent/open、pending targetなしの評価順を維持し、FileObject未結合時だけ`NO_PENDING_UNBOUND_*`診断へ接続した。lease snapshot、event予約QPC、current再open、deferred exact 1完全tuple/QPC、current identityを順に検証し、その後だけ既存の保持process inspectionを用いてidentity、wait、alive時Jobを評価する。3異常、process世代不一致、signaled、Job escape、candidateを固定14 stageへ限定し、認可helper、lease binding、容量actual、候補・公開条件は変更していない。

native規則試験は261件へ拡張し、TypeScript bridge/runnerは96〜112文字のexact codeだけを受理して旧exact・未知suffixをgeneric化する。Program SHA-256は`816c77dd1ff28498d271273d69f40e0aa5dd6d9585d48bc74c0f58ad0b18837d`、binary SHA-256は`903b4272f3daa3ae44c2e7ec82d046a62249de594d6745630f25ddeb20671c71`、75,048,796 bytesである。hosted再試験と独立受け入れ判定は後続タスクで実施する。

## 現行Program pin hosted follow-up

後続fail-closed guardを含むProgram SHA-256 `c3ac87d7f894f1723adee5f55555c03efb9b0a3c3c803c47fa682a50966231fc`で3 attemptを実行した。run `31237067677`は`WRITE_ACTIVE_PRODUCER_AFTER_RESERVATION_BIRTH`、run `31237211590`は`COMPLETED_NO_LEASE_DIRECTORY_HANDOFF_CANDIDATE_AMBIGUOUS`、run `31237375017`は`ACTIVE_DIRECTORY_HANDOFF_ELIGIBLE_AMBIGUOUS`へ安全停止し、いずれもT-109固有stageへ未到達だった。全attemptでcandidate branchは作成されず、Pages deployはskipである。

follow-up retry limit 3に達したため、T-109はQ-044へエスカレーションする。認可・lease binding・容量actual・候補保存・公開条件を変更せず、決定的なhosted影響確認方法を別変更管理で設計するか判断するまで本変更を`in-review`とする。
