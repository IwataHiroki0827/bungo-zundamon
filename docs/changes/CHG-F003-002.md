---
id: CHG-F003-002
status: done
---

# CHG-F003-002: 公開baselineの追記可能manifest投影を明確化

| 項目 | 内容 |
|---|---|
| 発生日 | 2026-07-26 |
| 起票契機 | T-041独立受け入れで固定F002 published baseline検査の未結合と集約manifestの矛盾を検出 |
| レベル | requirement |
| 状態 | done |

## 変更理由

`REQ-F003-001`は既存provenanceのpath・bytes・SHA-256をexact維持すると定める一方、`REQ-F003-017`は3人目の作者画像由来をcreditsで確認可能にする。既存`content/artwork-provenances.json`は作者ごとのentryを追記する索引であり、ファイル全体のbytes不変とF003 entry追加は同時に成立しない。また、F002公開後のmanifestへ追記された`publishedAt/status/evidenceSha256`を再構築Catalogへ流すと、固定v0.2.0 Catalog projectionから逸脱する。

## tm.jsonによる影響範囲

起点は`REQ-F003-001/017`、`DES-F003-002/009`である。直接影響は`FUN-F003-005/022/024`、`UT-F003-005/022/024`、`IT-F003-002/009/013`、`QT-F003-001/012`、作品受入`T-039〜T-041`、最終統合・試験・release`T-042〜T-044`である。

## 変更内容

- 単独assetは従来どおりpath・bytes・SHA-256をexact比較する。
- Catalogの既存F002 batch metadataは固定release commitのprojectionを使用し、後続published記録を再構築へ逆流させない。
- 追記可能な作者画像集約manifestは、固定F001/F002 entryをcanonical exactで維持し、検証済みF003 entryの追記だけを許可する。
- 固定published baselineのproduction verifierをwork preview、作品受入、最終統合、dist、release verificationのhash chainへ結合する。

## 承認

プロジェクトオーナーが本会話で「今回はすべてok」「手動確認は不要」と明示しているため、要求ゲートの承認として記録する。既存F001/F002 entryの意味・値・参照を変更するものではない。

## 完了条件

- 独立受け入れで`PUBLISHED_BASELINE_MISMATCH`が解消し、既存entry改変fixtureは拒否される。
- T-041の作品証跡を再生成し、published invariantを含むaccept hash chainが冪等に収束する。
- 全Vitest、typecheck、lint、build、trace_checkがPASSする。

## 完了結果

- 完了日時: 2026-07-26T20:19:06+09:00
- 固定v0.2.0 baselineをwork preview、作品受入、最終統合、dist、release verificationへ結合した。
- 既存F001/F002作者entry改変fixtureを`ARTWORK_PROVENANCE_PROJECTION_MISMATCH`で拒否し、検証済みF003 entryの追記だけを許可した。
- T-041の独立再受入はHigh/Medium/Low 0件でPASSした。
- 全46 test files / 840 tests、typecheck、lint、build、trace_checkがPASSした。
