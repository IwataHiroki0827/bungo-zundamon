---
id: CHG-F005-004
feature: F005
level: design
status: in-review
date: 2026-07-29
---

# CHG-F005-004 hosted Windows候補生成workflow

## 変更理由

ローカルMedium integrity processはkernel ETWを開始できず、T-070 production runnerは音声生成前に安全停止していた。CHG-F005-003のActions run `30445446783`で、固定native binary、容量ABI v3、kernel FileIO ETWの正常系をGitHub hosted Windows上で確認できた。

手動UACや監視なしfallbackを使用せず、同じ安全境界のまま「夢十夜」62音声と作品単位atomic受入を実行するため、通常Pages buildとは分離した一回限りの候補生成workflowを追加する。

## 変更内容

- `feature/F005`でworkflow自身が追加・変更されたpushだけを起動条件とする。`workflow_dispatch`、schedule、main、tagからは起動しない。
- checkout、Node、npm lock、native guard、VOICEVOX ENGINE `0.25.2` Windows CPU asset URL・SHA-256を固定する。canonical JSON、承認Markdown、ProjectFactory YAML、native guardのsource・project・build scriptはGit属性でLF checkoutへ固定し、raw bytes検証をOS設定から分離する。
- native build evidenceは追跡済みbytesを保持し、生成結果との意味的完全一致を検証した場合だけ元のexact bytesへ戻す。実値差を表現差として隠さない。
- 作業ドライブは開始時10 GiB以上、ENGINE展開後5 GiB以上を要求する。1,802,304,190 bytesの圧縮物はSHA検証後に展開し、直ちに削除してActions cacheへ保存しない。
- ENGINEはhidden processとして`127.0.0.1:50021`だけで起動し、runnerが版・speaker UUID/name・style ID/nameを実APIへ照合する。
- production runnerは`000799`だけを処理し、native exact 5 phase、closed journal、actual容量、metadata finalizeまで完了させる。
- `public/`差分0、manifest `000799=accepted`、accepted audio 62 WAV、変更path allowlistを自動検証する。
- checkout credentialは永続化せず、供給物・ENGINE・runner実行中にwrite tokenを環境やGit設定へ渡さない。ENGINE停止後の最終検証・保存stepだけへwrite tokenを渡す。
- runner結果はbuild logと混在しない`F005_RESULT_JSON=` marker付き1行JSONとしてexact 1件を要求する。
- native build直後かつVOICEVOX取得前に、HEAD・symbolic branch・ignored外の作業ツリーを再検証する。各Git commandは`Start-Process`の終了値を明示検査し、stdoutなしのGit異常も拒否する。状態不一致時は変更pathだけをBase64化して記録し、大容量取得前に停止する。
- runnerは`Start-Process -Wait -PassThru`の`ExitCode`でnative非0終了を捕捉する。失敗時はwrite tokenのないstepでstdout/stderr末尾64 KiBまでをBase64化してログへ出し、workflow command注入を避けながら使い捨てrunnerの診断情報を残す。
- 成功時だけsource commit固有の`candidate/f005-t070-{40桁SHA}`へcommitする。既存候補branch、部分生成、失敗、allowlist外差分ではpushしない。
- Pages deploy、main、tag、既存branch、公開変数を変更しない。

## tm.jsonによる影響範囲

- 要求（内容不変）: `REQ-F005-011`、`REQ-F005-012`、`REQ-F005-013`
- 設計: `DES-F005-006`、`DES-F005-012`、`FUN-F005-047`
- 試験: `UT-F005-047`、`IT-F005-005`、`QT-F005-008`、`QT-F005-009`
- 実装・再試験: `T-070`、`T-078`

## リスクと対策

- 大容量取得: disk guardと圧縮物即時削除、cache非保存でrunner全体停止を防ぐ。
- supply chain: 公式release固定URLとGitHub asset digestを照合し、差分を展開しない。
- 権限: checkoutは`persist-credentials: false`とし、jobのwrite tokenはENGINE停止後の最終保存stepだけでHTTP extraheaderへ一時注入する。Pages/id-token/secrets権限を与えない。
- 部分生成: workflow failure時はcommit/push stepへ到達させず、使い捨てrunnerとともに破棄する。
- 公開誤反映: `public/`差分0を要求し、候補branch名をsource SHAへ固定する。通常Pages deploy条件はmainの承認SHAだけなので候補branchを公開しない。

## 承認

既存要求、VOICEVOX tuple、容量境界、作品受入条件を変更しない設計レベルの実行環境追加であるため、要求承認ゲートは設けない。設計・試験仕様の独立再レビュー、静的workflow試験、hosted実行と候補実物受入が完了するまで`in-review`を維持する。

## 独立再レビュー

- 判定: PASS
- 重大度: High 0件、Medium 0件、Low 0件
- 確認事項: runner結果markerのexact 1行抽出、checkout credential非永続化、最終保存stepだけへのwrite token受渡し、一時HTTP extraheaderによるpush
- 独立試験: 対象2ファイル、9件すべてPASS
- rename相関追加レビュー: 初回REDOのMedium 1件（notice完了時採番によるETW順序逆転可能性）を、raw rename受信時のsequence予約と相関完了までの後続mutation拒否で解消した。再判定PASS、High 0件、Medium 0件、Low 1件。残るLowはWindows ETW上のnotice先行/ETW先行・旧名/新名・競合の完全動的試験であり、次のhosted Actions実行を非省略ゲートとする。
- FileObject再利用追加レビュー: Cleanup済みpointerの無条件再登録競合をREDO Medium 1件として検出し、Deferredからpointerを除去して現存する同一identity対応だけを更新するよう是正した。再判定PASS、High 0件、Medium 0件、Low 1件。残るLowは22件超のCleanup/Delete順序を含むhosted ETW integrationで確認する。
- manifest chain追加レビュー: 既到達stageの無条件skipによる旧voice evidenceと新session音声の混在をREDO Medium 1件として検出した。output SHA・件数・tool version・stage固有参照の完全一致時だけskipするよう是正し、再判定PASS、High 0件、Medium 0件、Low 0件。

## Hosted実行履歴

- Actions run `30446900688`: `Verify clean source identity`で安全停止。候補branch不在を正常条件として`git ls-remote --exit-code`の終了値2で表現したため、hosted PowerShellのnative commandエラー処理と競合した。音声生成、候補commit、pushは未実行。
- 是正: `--exit-code`を使わず、終了値0のref一覧を取得して0件を不在、1件以上を既存候補として判定する。
- Actions run `30447110475`: 固定ENGINE起動後の`Run T-070 production pipeline`で安全停止。失敗時stderrをrunner内だけに保持していたため、原因情報は候補branchへ保存されず破棄された。候補commit、pushは未実行。
- 是正: `Start-Process -Wait -PassThru`でnative終了値を明示取得し、token非保持stepでrunner stdout/stderrの末尾64 KiBをBase64化して失敗ログへ出す。workflow commandとして解釈されない形で次回診断可能にし、native終了7の回帰試験を追加する。
- Actions run `30447675936`: 診断経路が動作し、runnerの`cleanHead`が`F005_REGISTRY_CONTROL_INVALID`で停止したことを確認。checkout時点では`feature/F005` branch上だったため、native build後・VOICEVOX取得前のGit状態を追加検査して原因pathを分離する。候補commit、pushは未実行。
- Actions run `30448220756`: VOICEVOX取得前preflightが作動し、`native/f005-guard/build-evidence.json`だけの書換えを検出して停止。候補commit、pushは未実行。
- 是正: native build前の追跡済みevidence bytesを保持し、生成後JSONとの意味的完全一致を検証する。一致時だけ元のexact bytesを復元し、PowerShell表現差を除去する。フィールド値差は復元せず停止する。
- Actions run `30448569641`: native build evidenceの意味差を検出してVOICEVOX取得前に停止。Windows checkoutの改行設定からworktree bytes hashを分離するため、`native/f005-guard/**`をGit属性でLFへ固定する。再発時は差分field名だけをBase64診断する。
- Actions run `30448900994`: native evidenceとpreflightはPASSし、runnerがcandidate registryのcanonical JSON bytes不一致で停止。候補commit、pushは未実行。
- 是正: canonical JSONをOS設定に依存させないため、`*.json`をGit属性でLF checkoutへ固定する。
- Actions run `30449304206`: canonical JSON読込はPASSし、F004承認Markdownのraw SHA不一致で停止。候補commit、pushは未実行。
- 是正: 承認対象Markdownとqueue/registryを含むProjectFactory YAMLをLF checkoutへ固定する。
- Actions run `30449632715`: runnerは終了値0まで到達したが、stdoutの結果markerを行頭で0件と判定して停止。候補commit、pushは未実行。
- 是正: 直前build logの末尾改行有無に依存させず、結果markerの前後へ改行を付与する。marker件数・内容不一致でも上限付きBase64診断を残す。
- Actions run `30450036499`: runner終了値0、stdout空、stderrはNode警告だけでmarker 0件。native guardのstdin終了後にexit listenerを登録する競合により、高速exitを取り逃がした未解決Promiseの途中でNodeが自然終了した挙動と一致する。候補commit、pushは未実行。
- 是正: guardが既に終了済みなら終了値を即検証し、未終了時はexit/error listenerを先に登録してからstdinを閉じる。高速終了を16回反復する回帰試験を追加する。
- Actions run `30450593618`: listener順序・再検査修正後もrunner終了値0、stdout空、marker 0件を再現。候補commit、pushは未実行。
- 是正: context、voice、offline build、preview、stage、session close前後、actual、finalize、結果出力の固定checkpointをstderrへ記録し、失敗時Base64診断で終了境界を特定する。
- Actions run `30450958435`: `context-loaded`後・`voice-generated`前で終了。音声phase例外時の`session.abort()`がkill-on-close Jobでroot Nodeも停止するため、元例外のthrow・stderr出力前に終了していた。候補commit、pushは未実行。
- 是正: containmentを維持したまま、既知集合へ意味的に限定した例外名・通信/OS code・F005ソースファイル名とError cause一段だけを固定1行JSONとしてstderrへ出す。message、行番号、未知の相対path、絶対path、環境値は出力せず、flushの上限待機後に成否を問わずfinallyでabortする。Actionsへは既存の上限付きBase64だけを出す。
- Actions run `30452244932`: 安全診断により`F005VoiceError`相当、`f005-voice.ts`内、runner側`TypeError`原因と特定。version/speakers/audio query/synthesisのAPI境界までは未分類。候補commit、pushは未実行。
- 是正: runnerの4つの固定VOICEVOX API境界を、既知集合に含めた固定name/codeで包む。自由文字列を増やさず、失敗したAPI境界だけを次回診断へ出す。
- Actions run `30452664709`: `F005_VOICE_GENERATION_INVALID`のcauseが`F005_ENGINE_VERSION_FAILED`と確定。直前のPowerShell `/version` probeは成功している。候補commit、pushは未実行。
- 是正: 4つの固定VOICEVOX API境界をrequestとresponse parse/bodyへさらに固定分類し、自由文字列を増やさずNode HTTP層と解析層を切り分ける。
- Actions run `30453062191`: `F005_ENGINE_VERSION_REQUEST_FAILED`と確定。PowerShell probe成功後もNode 24の`fetch`通信層だけが失敗した。候補commit、pushは未実行。
- 是正: loopback transportをredirectしない標準`node:http`へ置換し、2xx必須、API別response上限、120秒socket timeout、既存外側timeoutを維持する。
- Actions run `30453785245`: `node:http`でも`F005_ENGINE_VERSION_REQUEST_FAILED`となり120秒socket timeoutまで待機。別step内のPowerShell probe成功後、production stepではengineへ接続できず、step境界でengine processが失われていると判定。候補commit、pushは未実行。
- 是正: engine起動、PowerShell probe、Node runner、engine停止を単一Actions stepの`try/finally`へ統合し、step境界でのprocess消失を排除する。probeは各接続/応答2秒、Stopwatchによる実時間120秒deadlineとし、応答停止時もfinallyへ到達させる。
- Actions run `30454585033`: engine同一step化後はversion requestを通過し、native guard内のvoice mutation境界で`F005_VOICE_GENERATION_INVALID`となった。候補commit、pushは未実行。
- 是正: native recorderのbegin/observe/end backend失敗を固定`F005VoiceError` codeへ分類し、既知のnative error name/code一段だけを安全診断へ追加する。
- Actions run `30455125486`: `F005_VOICE_NATIVE_OBSERVE_FAILED`、cause `F005_CAPACITY_IPC_FAILED`と確定。照合待ち相当の時間後に停止した。候補commit、pushは未実行。
- 是正: native replyの自由文字列をnotice未照合・ETW観測失敗・その他guard拒否の固定codeへ分類し、秘密を出さずobserve失敗原因を確定する。
- Actions run `30455642630`: `F005_VOICE_NATIVE_OBSERVE_FAILED`、cause `F005_CAPACITY_ETW_OBSERVATION_FAILED`と確定。engineはnative Job外PIDのまま、guard対象workspace内`.cache/voicevox-hosted`へstdout/stderrを書いていたため、ETWのJob membership fail-closed条件に抵触したと特定。候補commit、pushは未実行。
- 是正: VOICEVOX archive、展開先、working directory、stdout/stderrをすべてguard対象外の`RUNNER_TEMP/f005-voicevox-hosted`へ移し、workspace変更主体を登録済みworkerへ限定する。
- Actions run `30456210640`: engine I/O隔離後も`F005_CAPACITY_ETW_OBSERVATION_FAILED`を再現し、外部writerだけが原因ではないと判定。候補commit、pushは未実行。
- 是正: native guardの既知ETW failureをPID membership、phase外event、file identity、buffer/sequence等の固定集合へ細分化し、自由文字列を出さず実原因を確定する。
- Actions run `30456862599`: `F005_ETW_FILE_IDENTITY_MISSING`と確定。ETW callbackが作成直後の実体を再オープンする際、メタデータ照会だけにもかかわらず`GENERIC_READ`を要求していた。候補commit、pushは未実行。
- 是正: `CreateFileW`のdesired accessをmetadata-onlyの`0`へ狭める。reparse pointを追わないopen、share read/write/delete、hardlink数、FileId128、allocated lengthの検証は維持し、作成直後handleとの不要なaccess競合を排除する。
- 再現build: SDK `9.0.316`、runtime `9.0.18`、`win-x64` self-contained、固定`PathMap`でbuild/verify PASS。新しい固定binaryは74,958,684 bytes、SHA-256 `720f2ca4448fb4bc95c902d9fa45ce2f4ea35185ab07f1c9cbaebdcb8ee23e0c`。
- Actions run `30457930490`: metadata-only handleでも`F005_ETW_FILE_IDENTITY_MISSING`を再現し、GENERIC_READ競合説を否定。候補commit、pushは未実行。
- 是正: runner stdout/stderr/resultも`RUNNER_TEMP`へ移し、engine確認、native phase、staging root、query/synthesis/WAV、temporary create/observe、rename/observeの固定checkpointで失敗したmutation境界を特定する。
- Actions run `30458659099`: staging root、temporary WAVの作成・ETW照合まではPASSし、temporary WAVを正式名へrenameした直後だけ`F005_ETW_FILE_IDENTITY_MISSING`で停止。候補commit、pushは未実行。TraceEventの`FileIOInfoTraceData.FileName`はFileKey/FileObjectの内部対応表から解決され、rename event自体が新名を保持しないため、旧pathはrename完了後に再open不能、FileObject対応がない場合はidentityを得られない構造と確定した。
- 是正: ETW renameを受信時にsequence予約して旧path・既知FileId・worker/phase/timeで一時保留し、認証済みnoticeの新pathをmetadata-onlyで再照会する。旧FileIdと新FileIdが完全一致した場合だけfrom/to付き観測を確定し、未照合、相関中の後続mutation、FileId不一致はfail-closedとして観測順序を逆転させない。
- Actions run `30461113362`: rename相関は21音声連続でPASSし、22音声目のrename noticeだけ`F005_CAPACITY_NOTICE_UNMATCHED`で停止。候補commit、pushは未実行。閉じた過去ファイルのFileObject対応を保持し、kernel pointer再利用時に現在のETW旧pathスナップショットより古い対応を優先する構造と一致した。
- 是正: FileIO/CleanupでFileObject対応を破棄し、rename元はFileObject対応よりETW旧pathに紐づく既知FileIdを優先する。rename確定時は現存する同一identity対応だけを更新してCleanup済みpointerを再登録しない。新pathの同一FileId再照会とfail-closed条件は維持する。
- Actions run `30462205641`: 62音声すべての生成、temporary照合、rename照合、native voice phase終了をPASSした。直後のmanifest段階遷移で`batch.ts`が証跡chain不整合を拒否して停止し、候補commit、pushは未実行。
- 是正: reviewed以降のstage evidence `inputHashes`を、現在manifest SHAだけでなく直前stageの全`outputHashes`へ結合する。pendingからvoicedまでの純粋遷移関数をproductionと試験で共有し、4段階の実行回帰試験を追加する。既到達stageの再開skipは、output SHA・件数・tool version・forecast/voice evidence参照が今回入力と完全一致する場合だけ許可し、新sessionの音声と旧manifest evidenceの混在を拒否する。
- 再現build: SDK `9.0.316`、runtime `9.0.18`、`win-x64` self-contained、固定`PathMap`でbuild/verifyする。更新後の固定binaryは74,966,876 bytes、SHA-256 `6bafe6bdbac56e69588b330260632acda970922283912e121c224086a81cc656`。
- Actions run `30463463812`: 62音声生成、manifestの`voiced`遷移、Vite buildまで完了後、F001 baseline preflightが`F001_ASSET_HASH_MISMATCH`で停止。候補branchは作成されず、公開treeも未変更。F001 allowlist中で唯一Git改行属性が未指定だった1-byte LFの`public/.nojekyll`がWindows checkoutでCRLF化される条件を除去するため、同pathを`.gitattributes`で`text eol=lf`へ固定し、hosted workflow契約試験へ回帰検査を追加する。
- 独立レビュー: `public/.nojekyll`の実体が1 byte LF、F001/F002固定hashと一致し、変更規則が同pathだけへ限定されることを確認。workflow変更は再実行用commentだけで、High 0 / Medium 0 / Low 0のPASS。
- Actions run `30464470912`: checkout、native再現build、VOICEVOX固定SHA検証をPASSし、8音声のtemporary/rename観測を完了。9音声目のtemporary write直後にETW観測が汎用`F005_CAPACITY_ETW_OBSERVATION_FAILED`で停止した。候補branchは作成されず、公開treeも未変更。自由文字列・HRESULT・pathを出さず、既存native replyを未知event、consumer、callback、consumer stop、session stopの固定codeへ分類して次回診断する。
- 独立レビューで、分類後もtop-level未処理例外がraw native messageと絶対stack pathをstderrへ再出力し、workflowのBase64診断へ混入し得るMedium指摘を受けた。CLI終端でallowlist済み診断以外のraw例外を表示せず終了code 1だけを返し、秘密相当の引数と絶対pathがstderrへ出ない実process回帰試験を追加する。
- 再レビュー: 安全診断のflush後にraw例外を抑止し、同期throwと非同期rejectionの双方が終了code 1へ収束することを確認。ETW分類順序と既存codeにも回帰なく、High 0 / Medium 0 / Low 0のPASS。
- Actions run `30465357312`: 43音声のtemporary/rename観測を完了し、44音声目のrename直後に`F005_ETW_CALLBACK_FAILED`で停止。候補branchは作成されず、公開treeも未変更。callback内の非Guard例外を、型だけに基づくaccess/argument/disposed/I/O/overflow/state/unknownの固定codeへ分類し、message・HRESULT・pathをnative replyへ含めず次回診断する。
- 再現build: SDK `9.0.316`、runtime `9.0.18`、`win-x64` self-contained、固定`PathMap`でbuild/verify PASS。固定binaryは74,966,876 bytes、SHA-256 `43d3b8f0764e14e96d4271d74ef0ecd4bdfe37c44ec4fbc6ff6e1597c321be47`、Program SHA-256 `2bab492bb8feb9026a152c203e8511f37d8b76269a720fa81c1b6d8f2d4fcf88`。
- 独立レビュー: 例外型の派生順、固定codeへの収束、Poison後のfail-closed、TS分類・安全診断・全pin同期を確認し、High 0 / Medium 0 / Low 0のPASS。
- Actions run `30466381295`: 62音声の生成・temporary/rename観測、manifest連鎖、offline buildをすべてPASSした。直後の次noticeで`F005_ETW_FILE_IDENTITY_MISSING`を検出して停止し、候補branchは作成されず公開treeも未変更。FileIO/CleanupでFileObject対応を破棄した後のdeleteは実体を再openできないため、同じ正規化pathで直前に検証済みのFileId対応をstale pointerより優先して相関する。
- 再現build: SDK `9.0.316`、runtime `9.0.18`、固定`PathMap`でbuild/verify PASS。固定binaryは74,966,876 bytes、SHA-256 `19647970f409781ab6d2382d873abde52a98a4a21fe3b41eb20c8d25a3298218`、Program SHA-256 `96f60c41297f405ea2a8a54e03bee92e3d1fc62a8c31ca1fc8f9fc53457e28cb`。
- 独立レビュー: current再照会を最優先し、同pathの検証済みidentity、再利用され得るpointerの順で相関するため明確なfail-openはない。High 0 / Medium 0 / Low 1のPASS。Lowはhosted実ETWでoffline-build delete/Cleanup順を再確認するintegration残であり、次回隔離実行を証跡とする。
- Actions run `30467479439`: 31音声のtemporary/rename観測を完了し、32音声目のtemporary write直後に既知型外の`F005_ETW_CALLBACK_FAILED`で停止。候補branchは作成されず公開treeも未変更。callbackの未知例外をmessage・型名・HRESULT・pathへ依存せず、normalize/identity/correlation/capacity/record/journalの固定段階へ分類する。
- 再現build: SDK `9.0.316`、runtime `9.0.18`、固定`PathMap`でbuild/verify PASS。固定binaryは74,966,876 bytes、SHA-256 `4ed12d179364012a01adeff3082698a4af08d5609702e662951b3c6455b468e3`、Program SHA-256 `e61fe79fcb702ae53e951755794957ee32f77d2802147e4540b18c4da6345376`。
- 独立レビュー: 非rename経路の段階、固定code、情報非開示、fail-closed、pin同期を確認しHigh 0 / Medium 0 / Low 1のPASS。Lowはrename即時相関helper内の未知例外が詳細段階でなくcorrelationへ集約される診断精度だけで、安全性と今回のtemporary write診断には影響しない。
- Actions run `30468445993`: 35音声のtemporary/rename観測を完了し、36音声目のtemporary write直後に`F005_ETW_CALLBACK_NORMALIZE_FAILED`で停止。identity照会前の処理を追跡し、registerSelfでJobへ割当・所属確認済みのroot PIDまで全ETW eventごとに`Process.Handle`を再取得していたことを特定。named pipe peerとしてsession中生存しbreakaway不能なrootだけは初回Job所属証明を再利用し、子PIDは従来どおり最初のETW eventでJob所属を確認する。noticeは同じ生存pipe peer PIDだけを許可する。
- 再現build: SDK `9.0.316`、runtime `9.0.18`、固定`PathMap`でbuild/verify PASS。固定binaryは74,966,876 bytes、SHA-256 `780c2b127c4041c6a80f8fe295ed7c251ae86ad9999b7223c977d8caaf63a922`、Program SHA-256 `ddb1db9684ea11e79b879494c3593951a3a04636fe7a6d69f9f8e373c224ce81`。
- 独立レビューで、PIDだけを保持するとpipe切断後のPID再利用をrootとして受理し得るMedium指摘を受けた。registerSelf時に割当済みprocessの独立handleをguard終了まで保持し、root event/noticeごとに同じprocessの生存とJob所属を確認する。pipe EOFはclose後以外をPoisonし、closeはETW停止前後の両方でroot handleを再確認してPID再利用raceを封じる。
- 再現build: SDK `9.0.316`、runtime `9.0.18`、固定`PathMap`でbuild/verify PASS。固定binaryは74,966,876 bytes、SHA-256 `6138cae88a7a6efb1ce54ebdf8bb62b5d84371f14eb5ad1d967c8bf9708bc77a`、Program SHA-256 `f9a714a3a84f80c1987940808d8abcd19a28b71fa5a6194edfaf72ee1dc8477f`。
- 再レビュー: retained process handle、EOF Poison、ETW停止前後のroot再確認でPID再利用raceが閉じ、lock順にも循環がないことを確認。High 0 / Medium 0 / Low 1のPASS。Lowは実processのEOF/PID再利用動的integrationがhosted確認待ちである点のみ。
- Actions run `30470045586`: root handle固定後は62音声のtemporary/rename観測とnative voice phase、manifest連鎖、offline buildをすべてPASSした。直後の次noticeでbuild中に蓄積した`F005_ETW_FILE_IDENTITY_MISSING`を検出して停止。候補branchは作成されず公開treeも未変更。root `Process.Handle`再取得由来のランダムcallback失敗は再発していない。次は固定GuardExceptionにもevent種別と処理段階を付与し、Viteの短命fileにおけるcreate/write/rename/deleteのどの相関が欠落したかを安全に確定する。
- 是正: `ETW_FILE_IDENTITY_MISSING`を固定のevent種別（create/write/setinfo/rename/delete）と処理段階（identity/correlation）の直積へ分類する。絶対path、FileId、PID、例外message、HRESULTは出力せず、直接Poisonする相関欠落と`TryInspect`由来のGuardExceptionを同じ有限集合へ収束させる。
- 再現build: SDK `9.0.316`、runtime `9.0.18`、`win-x64` self-contained、固定`PathMap`でbuildする。更新後の固定binaryは74,966,876 bytes、SHA-256 `49b7a3a6df9c936eb95fac643deb0c972d3f5eadc09967065daaa8d671fe7188`、Program SHA-256 `d53a5413ccec228b90d377ccae235751051c011c3fbd11b0c293e91a08baf2cb`。
- 独立レビュー: `ReceiveNotice`から`CompleteDeferredRename`へ入る通常の保留rename相関だけ、`TryInspect`のidentity欠落が旧generic codeのままpipe側へ抜けるMedium 1件を検出。相関helper自身で固定`RENAME_CORRELATION`へ変換し、ETW callback内とnotice内の両経路を同じ有限集合へ収束させる。
- 是正後の再現build: 固定binaryは74,966,876 bytes、SHA-256 `5917a64ec71f95d160b2f93e464fedd542ecf1d51406bec0949f60b6b1d73b03`、Program SHA-256 `4b8bbb913ad29d3ab6c2da48863efd70a185bdb497c469d69688fb5735e6eca0`。
- 再レビュー: `TryInspect`のnull戻りは分類できたが、handle取得後のmetadata API失敗が投げる同じGuardExceptionはnull合体演算子を通らず旧generic codeへ抜けるMedium 1件を検出。rename先照会を専用helperへ集約し、null戻りとmetadata API由来throwの両方を固定`RENAME_CORRELATION`へ変換する。
- 二次是正後の再現build: 固定binaryは74,966,876 bytes、SHA-256 `ce74bb6e4bf9aaef45b2a663f4927c73645a53dfa6cf69ce5bdf8e53257989f9`、Program SHA-256 `581da334d33cf7ae91630c4f650042ece29ee18f413ca94d3fc2e943c49cca60`。
- 最終独立再レビュー: `InspectDeferredRenameTarget`がnull戻りとmetadata API由来throwを同じ固定codeへ収束させ、他のidentity unsafe・allocated length・path・rename mismatch codeはfilter外で保持することを確認。High 0 / Medium 0 / Low 1のPASS。Lowはnull/metadata API fault injectionの動的試験がなく、次のhosted Windows統合実行で補完する点のみ。
- 影響試験: native再現build、重点112試験、全1257試験（`--maxWorkers=2`）、型検査、lint、diff-checkをPASS。公開treeとcandidate branchは未変更。
- Actions run `30475887596`: 62音声の生成・rename観測、native voice phase、manifest連鎖、Vite build 697 files / 229,936,268 bytesをPASSした。build phase中のETW callbackが`F005_ETW_FILE_IDENTITY_MISSING_CREATE_CORRELATION`で停止し、候補branchは作成されず公開treeも未変更。低レベルeventごとに増大する全journalをdurable rewriteしていたためETW consumerがViteより遅れ、create callback時には短命実体を再openできない構造と確定した。
- 是正: ETW callbackではFileId/allocated lengthと観測をmemoryへ即時捕捉し、open journalのdurable rewriteをnotice・phase境界へ限定する。open journalは異常終了時に受理されず、closed sealは従来どおり全観測をfsyncする。`endPhase`はgate外でETW bufferをflushし、配信event countが2回連続で静止したことを確認してからphase境界を永続化する。drain失敗・2秒超過は固定codeでfail-closedにする。
- 再現build: 更新後の固定binaryは74,970,972 bytes、SHA-256 `10c5cc8402228a2d0c08b49fb2ee8ebdd6892a8fe4cbe1507464ccb55b54afd3`、Program SHA-256 `26bcef0f14b24ba910b1ef7df11310ba327342f4701117d4b44900673a36fecf`。
- 独立レビュー: `Flush`後の200ms静止はconsumer callback完了barrierではなく、遅延した旧phase eventを次phaseへ誤帰属してsealed journalへ入れ得るMedium 1件、20回×最大200msと「2秒超過」記載が不一致なLow 1件を検出。
- 是正: 各phaseのUTC開始境界をnative内で固定し、ETW timestampが現在phase開始より古いeventは固定`ETW_EVENT_PHASE_TIMESTAMP_MISMATCH`でPoisonする。これにより静止判定後に遅延eventが到着しても新phaseへ帰属できない。静止待ちはworkspace関連callbackだけを数える最大2秒のliveness補助とし、安全性はtimestamp境界とphase外Poisonで担保する。
- 再レビュー是正後の再現build: 固定binaryは74,970,972 bytes、SHA-256 `734bbf796b8afea3700e0a043a9bb81cd98ed6dbce6e0b4e7756632e050a653f`、Program SHA-256 `4f9c685bd35b4753b4332d8492f02276f0114d6ca6c93da3b7210023779859cb`。
- 再レビュー: TraceEventの`TimeStamp`はQPCからsystem clockへ変換された値で、UTC境界とは開始時skew・経時driftがあり、安全なphase比較に使えないMedium 1件を検出。表示用UTCは維持し、安全境界を同一QPC clockの`Stopwatch.GetTimestamp()`と`TraceEvent.TimeStampQPC`の比較へ変更する。境界同値を含む旧eventは固定codeでPoisonする。
- QPC是正後の再現build: 固定binaryは74,970,972 bytes、SHA-256 `7c47f7cc15a628e82fdfb1c4f84b70c5749755bdab8a247096226aea362d002a`、Program SHA-256 `7ed69f9a9a8ee1e0a654509a5d0cc926b1f7b4984e776b5100ba4c2524cb67e2`。
- 最終独立再レビュー: 全5種のtyped FileIO callback、同一QPC境界、固定Poison code、UTC表示分離、pin同期を確認しHigh 0 / Medium 0 / Low 1のPASS。Lowは遅延旧event/QPC境界の動的注入試験がなく、次のhosted Windows統合実行で補完する点のみ。
- 影響試験: native再現build、重点113試験、全1258試験（`--maxWorkers=2`）、型検査、lint、diff-checkをPASS。
- Actions run `30478227773`: journal I/O除去で実行時間は短縮したが、音声終盤のrename直後に既知型外`F005_ETW_CALLBACK_NORMALIZE_FAILED`で停止。候補branchは作成されず公開treeも未変更。旧NORMALIZE段階にはpath正規化だけでなくlock状態、Job認証、phase境界が混在するため、固定STATE/AUTHORIZATION/PHASEへ細分化して次回の安全診断を一意化する。
- 再現build: 固定binaryは74,970,972 bytes、SHA-256 `6ff0b34c5f7e3854a611e554ddc3157b2b3e925cf479bc33036e322a1ee7b0f8`、Program SHA-256 `922cc3dca4f351abcdd27fb1ae7b1ed341fbfcddd12af60f912f644940cb02ad`。
- 独立レビュー: 既知例外型の優先、STATE/AUTHORIZATION/PHASEの未知例外固定分類、GuardException code保持、全layer/pin同期を確認しHigh 0 / Medium 0 / Low 1のPASS。Lowは各段階の動的fault injectionがなく、次のhosted Windows runで補完する点のみ。
- Actions run `30478894509`: 音声終盤のtemporary write直後に`F005_ETW_CALLBACK_AUTHORIZATION_FAILED`と確定。候補branchは作成されず公開treeも未変更。保持済みroot `Process`でもeventごとに`HasExited`を再照会してWin32例外化し得るため、保持handleへtimeout 0の`WaitForSingleObject`を使い、signaledでない場合だけ同じhandleのJob所属を照合する。PID再取得と新規handle取得は行わない。
- 再現build: 固定binaryは74,970,972 bytes、SHA-256 `2055bc3510bf2888facc6e429ce99cd6c82eabb884d92751429a089f3e33f345`、Program SHA-256 `5e201ee83ea1ee1457f9326f4dab89e32966b602fa0ead1ecfaab53f76a851ac`。
- 独立レビュー: root hot pathは改善したが、phase終了/closeの全process確認がPIDから別handleを再取得して`HasExited`を呼び、同じ例外と終了済みchildのPID再利用誤判定を残すMedium 1件を検出。
- 是正: childも初回Job認証時の`Process` handleをセッション終了まで保持する。Job列挙で初めて見つけたchildは同じhandleで生存・所属を確認できた場合だけ保持し、列挙後に終了してPIDが外部processへ再利用されても旧handleのsignaled状態から無視する。生存中の保持handleがJob外、またはJob照会失敗の場合だけ`JOB_PROCESS_ESCAPE`でPoisonする。
- child handle是正後の再現build: 固定binaryは74,970,972 bytes、SHA-256 `73528e5405afc1a3b4b2a2ba86a8567b04538531535ac61a0f8f70afdc6c7c6a`、Program SHA-256 `e38f71ea325e185074c98c09bdbcab645efffd5a90921504b9eb45031cedb739`。
- 自己点検で、phase終了/closeの`WaitForSingleObject`失敗を終了済みprocessと同じ扱いにしていた経路を検出。終了通知だけを正常終了とし、`WAIT_TIMEOUT`以外の異常値はescapeとしてfail-closedにする。
- fail-closed是正後の再現build: 固定binaryは74,970,972 bytes、SHA-256 `feeab7bfe2bbfa6e2ab9915dd2d9ff65ef30e8a479edabdaa5bef4406c0dca34`、Program SHA-256 `d7cb2c4a6be5b992c54fee947344a504acf7723265c2b3f9a4bf092aa25da029`。
- 最終独立レビュー: 同一handleでの生存・Job所属検証、外部PID再利用の拒否、`HasExited`/登録済みPID再取得の除去、辞書同期、wait/Job照会失敗のfail-closed、pin同期を確認しHigh 0 / Medium 0 / Low 2のPASS。Lowは、終了済みchildと同じPIDを正当な新childが稀に再利用すると安全側停止するliveness、およびwait結果・PID再利用の動的fault injectionがhosted確認待ちである点。
- 影響試験: native再現build、重点114試験、型検査、lint、diff-checkをPASS。直前のQPC/journal是正時点の全1258試験PASSに対し、今回差分はnative buildとsource契約試験で回帰確認した。
- Actions run `30480785599`: 保持handle是正でランダム`CALLBACK_AUTHORIZATION_FAILED`は解消したが、音声生成終盤のrename直後に固定`F005_ETW_PID_NOT_JOB_MEMBER`で停止。候補branchは作成されず公開treeも未変更。独立レビューLowで特定済みの、終了済みchildのPIDが正当な新Job childへ再利用された場合も旧handleをPID辞書へ永久保持して拒否する経路と整合する。
- 旧handleの終了だけを根拠に現在PIDへ置換する案は、外部processの遅延ETW event後に正当なJob childが同じPIDを再利用する逆向きraceで誤認可し得るため不採用とした。
- 最終是正: Windows SDKのSystem IO Providerを`EnableTraceEx2`で`EVENT_ENABLE_PROPERTY_PROCESS_START_KEY`付き有効化し、ETW eventのboot一意`ProcessStartKey`と、同じ保持handleへ`NtQueryInformationProcess(ProcessTelemetryIdInformation)`した値の完全一致でroot/childを認可する。開始直後にroot自身のprobe FileIOでevent key・handle key一致を実証し、欠落・不一致・10秒timeout・未probe phase開始を固定codeでfail-closedとする。TraceEvent 3.2.5の非公開`EVENT_RECORD`参照はwin-x64 ABI検査と実event probeの二段で検証する。
- ProcessStartKey是正後の再現build: 固定binaryは74,979,164 bytes、SHA-256 `00d93c987643361e8f5a51643da9792f5bb17745ca0a3e2edf781a106482e860`、Program SHA-256 `97235975ced91cb852de51ea7b61f9308a855d24fce5ccff4ff2ced26d3adced`。
- 独立再レビュー: TraceEvent 3.2.5実型との反射契約、System IO GUID/keyword/ProcessStartKey property、有限enable timeout、開始失敗時のsession/Job解放、EVENT_RECORD offset、ProcessTelemetry layout、PID再利用の両方向race、probe/IPC fail-closedを確認し、High 0 / Medium 0 / Low 1のPASS。残るLowはhosted OS上のroot probeと全pipelineの動的証跡だけであり、次のActions PASSまで本変更を`in-review`に維持する。
- Actions run `30485095402`: native probeと再現buildはPASSしたが、production runnerは`context-loaded`後・session開始中にroot Jobごと安全停止し、結果marker 0件となった。候補branchは作成されず公開treeも未変更。session開始失敗はguard停止でrootを終了するため既存のmain catchへ到達せず、固定原因codeが診断へ残らなかった。
- 是正: `startF005NativeCapacitySession`へ任意の固定code reporterを追加し、session開始例外を`F005NativeCapacityErrorCode`へ収束してstderr flush完了後にだけkill-on-close guardを停止する。runnerは`F005_NATIVE_START_FAILURE=<固定code>`だけを出力し、自由message・path・HRESULTは追加しない。
- Actions run `30485762178`: 先行診断が作動し、session開始時root probeで`F005_ETW_PROCESS_START_KEY_MISSING`を確定した。System IO Providerの有効化自体、kernel FileIO、root PID、probe pathは到達したが、GitHub hosted OSはFileIO eventへ要求済み`ProcessStartKey`拡張データを付与しなかった。候補branchは作成されず公開treeも未変更。
- 是正: System IO ProviderのFileIO監視にkernel `ProcessStart`を追加し、session開始後に生まれるchild/孫のpayload PIDとQPC開始境界を記録する。FileIO eventは最新開始境界より後で、同じ保持handleが生存してbreakaway禁止Jobに所属し、handleから取得したboot一意`ProcessStartKey`を固定できる場合だけ認可する。拡張event keyが存在する環境ではhandle keyとの完全一致も要求する。session開始前から生存するrootは保持handleの生存・Job所属とPIDで認可し、event keyがあれば追加照合する。これによりkey非付与環境を許容しつつ、同一session内のPID再利用による古いeventは新processのQPC境界以前としてfail-closedにする。
- 再現build: SDK `9.0.316`、runtime `9.0.18`、`win-x64` self-contained、固定`PathMap`で2回連続build/verify PASS。固定binaryは74,983,260 bytes、SHA-256 `adc57dae4612a5e3fd06ce128b8981c6512f15c4c5ed9cc2ac9536dc9ea08412`、Program SHA-256 `1dd0c2fcd0bb2289a20bba7c16b3b254b284c9d5c58ccf46ad7ced1ca073e354`。
- 独立レビュー: QPCだけのfallbackでは、旧generation Aのstart recordが残ったまま同じPIDをJob内generation Bが再利用し、BのProcessStart callbackより先にAの遅延FileIOが到着すると、event key非付与環境で現在のB handleへ誤結合できるMedium 1件を検出してREDOとした。また初回journal永続化失敗時にETW/pipe taskが匿名化し得るLow 1件を検出した。
- 是正: manifest provider `Microsoft-Windows-Kernel-Process`のprocess keyword/start event ID 1を10秒timeoutで有効化し、start payloadのboot一意`ProcessSequenceNumber`を記録する。現在のJob handleにも`ProcessTelemetryIdInformation`のbyte 40から同じsequenceを取得し、QPC開始境界に加えて完全一致を必須とした。これによりA record＋B handle、B record＋A遅延eventの両方向raceをfail-closedにする。TraceEvent `EnableProvider`のboolは成功値ではなくsession再起動有無のため判定から除外し、例外を有効化失敗として扱う。初回journalはETW thread/pipe task開始前に永続化する。
- 再現build: SDK `9.0.316`、runtime `9.0.18`、`win-x64` self-contained、固定`PathMap`で2回連続build/verify PASS。固定binaryは74,983,260 bytes、SHA-256 `36ac0edcd1719731eca4e24c6865e486b912a79e31fbea57878770519f6b4ec3`、Program SHA-256 `546e01c5e0cc951f30a66f67b49c72571d573e16285d4e5aeedfdc4af4fdcce4`。
- 独立再レビュー: A birth record＋B handleはsequence不一致、B birth record＋A遅延eventはQPC境界、B start欠落＋旧A recordもsequence不一致として両方向raceがfail-closedになること、初回journalがtask開始前であること、provider timeout/例外契約、Telemetry offset、全pin同期を確認しHigh 0 / Medium 0 / Low 1のPASS。LowはTypeScript回帰がC#実装を直接駆動するraw trace replayではなく、hosted Windows実contractが次のActions実行待ちである点のみ。
- 影響試験: native再現build 2回、重点27試験、全71 files / 1262試験、型検査、lint、diff-check、F005 trace checkをPASS。公開treeとcandidate branchは未変更。
- Actions run `30488207170`: pinned native build、source clean、VOICEVOX取得をPASS後、production session開始中に`F005_CAPACITY_IPC_FAILED`で安全停止した。候補branchは作成されず公開treeも未変更。provider開始後のpipe接続、root registerSelf、probe arm/write/verifyのtransport例外が同じgeneric codeへ収束し、失敗段階を一意に切り分けられないことを確認した。
- 是正: startup transportの各await直前に固定stageを記録し、自由message・path・HRESULTを出さず、guard channel、pipe connect、registerSelf、probe arm/write/verifyの6固定codeへ分類する。native guardが返した既存の固定codeは上書きせず優先し、診断codeのstderr flush後にだけkill-on-close guardを停止する。
- 独立レビューで、native guard生成がtry外にあり、稀な同期spawn例外だけ固定診断とcleanupを通らないLow 1件を確認した。guard生成も`guard-channel` stageのtry内へ移し、生成前失敗時はoptional cleanup、生成後失敗時は従来どおり診断flush後に停止する。
- 独立再レビュー: 同期constructor例外を含む6段階の固定code化、既存native code優先、秘密・自由文・path・HRESULT非出力、診断flush→pipe close→guard停止の順序、型/allowlist一致を確認しHigh 0 / Medium 0 / Low 0のPASS。重点33試験、型検査、lint、diff-checkも再PASS。
- Actions run `30488987297`: 固定診断によりproduction session開始は`F005_CAPACITY_IPC_CONNECT_FAILED`で停止したと一意に確定した。native側はpipe loopを`Task.Run`へ予約してから開始応答を返すため、task内の最初の`NamedPipeServerStream`生成よりNode接続が先行できた。候補branchは作成されず公開treeも未変更。
- 是正: 最初の`NamedPipeServerStream`をconstructorで同期生成し、そのinstanceをpipe loop taskへ渡してから開始応答を返す。task schedulerの遅延に依存せずOS上のpipe instanceが先に存在する。初期task生成失敗時は同期生成したpipeを破棄し、再接続用の後続instanceは前接続の終了後にloop内で生成する。
- 再現build: SDK `9.0.316`、runtime `9.0.18`、`win-x64` self-contained、固定`PathMap`で2回連続build/verify PASS。固定binaryは74,987,356 bytes、SHA-256 `9cf57a9e2651d959084c393ef55e0eaf41e887ddc141da56856a17d14351b829`、Program SHA-256 `d885e881dfe825a80d8445167e93b48eaced07a2346ce292f890485206074b9e`。
- 自己点検で、再接続用pipeを旧instanceの`await using` scope内で生成すると`maxNumberOfServerInstances=1`に抵触し得ることを検出した。接続処理を明示的なinner `await using` scopeへ入れ、旧instanceの非同期dispose完了後だけ次instanceを生成する。
- 最終再現build: 固定binaryは74,987,356 bytes、SHA-256 `dfc7dd230bfd152b3edc432fd34eb2ada87c1ddc08d4e356ba4da73e59d9be35`、Program SHA-256 `674eccdcfd4cabd43b2b2029d1d0ee6255c857dba3c46fec9e834193c31ba3a5`。SDK/runtime/RID/PathMap固定で2回連続PASS。
- 独立レビューで、peer identity失敗時の`continue`が破棄済みpipeを次周回で再利用するLowと、次instance生成直後のcancelで未使用pipeを残すLowを確認した。identity失敗はPoison後にpipe loop自体を終了し、loop条件を常時進入へ変更して、取得済みの全instanceをcancel済みでも必ず`await using`へ通して破棄する。
- pipe lifecycle是正後の最終再現build: 固定binaryは74,987,356 bytes、SHA-256 `fb2ccf7b6ca95cbe38d653a9bbcbe85b00bc29506f5f6da23a68572cade8a4c9`、Program SHA-256 `fa3a8fb218c3c0957bb524978adf9391de6185b174c004e7572058c7a05e92fa`。2回連続PASS。
- 最終独立再レビュー: 初期pipeの所有権移転、task開始失敗cleanup、旧instance破棄後の次instance生成、cancel後を含む全取得済みpipeのdispose、peer identity失敗時のPoison＋loop終了、capacity-start応答前のOS pipe ready保証を確認しHigh 0 / Medium 0 / Low 0のPASS。重点44試験、型検査、lint、diff-checkもPASS。
- Actions run `30489794479`: pipe接続、root Job登録、process identity probe、native voice phase開始、最初の音声temporary/rename観測をPASSした。2件目のtemporary write後に`F005_ETW_PID_NOT_JOB_MEMBER`で安全停止し、候補branchは作成されず公開treeも未変更。pipe readiness是正の実動PASSを確認した一方、同codeがbirth欠落、QPC境界、Job外、sequence/key不一致を一括しており次原因を一意化できない。
- 診断是正: 認可関数がfalse時に、root inactive、birth欠落、event境界以前、保持handle Job外、現在process取得不能、identity取得不能、sequence不一致、event key不一致の有限8理由を返す。native/TypeScript/runnerの固定code集合を同期し、PID、path、時刻、key、sequence、例外message、HRESULTは出力しない。認可成功条件は変更せず、失敗理由だけを細分化する。
- 再現build: 固定binaryは74,987,356 bytes、SHA-256 `32676e910dcb8dfc4d1c20e4f66b3e290c9a68bdfb8452c907773aac9fbff65f`、Program SHA-256 `1de2487801fff22c99a77f5086bb45cbab5dfac71dd7e8b8e82f9bca2cbf993f`。2回連続build/verify、重点44試験、型検査、lint、diff-checkをPASS。
- 独立再レビュー: 旧認可条件との分岐比較、false全8経路/success全経路のout初期化、固定literalだけからのcode生成、機微情報非出力、C#→TypeScript union→reply map→runner allowlistの1対1対応、全pinを確認しHigh 0 / Medium 0 / Low 0のPASS。
- Actions run `30490388572`: pipe/root probe/最初の音声temporary観測をPASSし、最初のrename直後に`F005_ETW_PID_NOT_JOB_MEMBER_BIRTH_MISSING`で安全停止した。session開始後のJob child世代不一致ではなく、session開始前から存在する別processのeventと確定した。候補branchは作成されず公開treeも未変更。
- 診断是正: birth欠落時だけ、PIDを出力せずSystem process（PID 0/4）か否かと、当該FileObjectが先行する認証済みeventで既に結合済みかを有限3理由へ追加分類する。認可結果は引き続きfalseのままとし、この段階ではSystem/結合済みeventも許可しない。
- 再現build: 固定binaryは74,987,356 bytes、SHA-256 `00b40be2940babf26996a1f4c2c7ab73a32aace75ba912df3f50fc11bcf9b2de`、Program SHA-256 `b6084725e5cc427c53c7f9e1bc4074a0395dd22341c063393054c0a4c7518abe`。2回連続build/verify、重点44試験、型検査、lint、diff-checkをPASS。
- 独立再レビュー: `filesByObject`のgate保護、認可false＋birth欠落時だけの分類、分類後の必須Poison/return、固定literalと機微値非出力、TypeScript/runnerの1対1対応、全pinを確認しHigh 0 / Medium 0 / Low 0のPASS。
- Actions run `30490876500`: root probeと最初の音声temporary観測をPASSし、rename直後に`F005_ETW_PID_NOT_JOB_MEMBER_SYSTEM_PROCESS_UNBOUND_FILE_OBJECT`で安全停止した。System processによる別FileObjectのeventと確定したが、操作種別と既知path結合可否が未確定。候補branchは作成されず公開treeも未変更。
- 診断是正: System processかつ未結合FileObjectのbirth欠落だけ、typed callback由来の固定操作5種と、正規化pathが`filesByPath`へ既登録かを固定10分類する。認可結果はfalseのまま維持し、PID、path、FileObject等の値は出力しない。
- 再現build: 固定binaryは74,987,356 bytes、SHA-256 `d33c42d3e86f8668f18db2d5fefbc4f69d7c40778c0094a27ec74e839ada9803`、Program SHA-256 `bbbc167a7648bb55c99fbfd430ef27eb686b604f52cc94b6630ded74c7e86be1`。2回連続build/verify、重点44試験、型検査、lint、diff-checkをPASS。
- 独立再レビュー: gate保護、System PID 0/4・birth欠落・未結合FileObjectへの限定、認可falseと必須Poison、将来操作の`UNKNOWN`収束、固定labelだけの非機微出力、10分類のTypeScript/runner 1対1対応、全pinを確認しHigh 0 / Medium 0 / Low 0のPASS。
- Actions run `30491886248`: source identity、再現native build、固定VOICEVOX、root probe、最初の音声temporary観測をPASSし、rename直後に`F005_ETW_PID_NOT_JOB_MEMBER_SYSTEM_PROCESS_UNBOUND_FILE_OBJECT_WRITE_KNOWN_PATH`で安全停止した。System processによる既知fileへの遅延writeと確定し、候補保存stepは未実行、公開treeも未変更。
- 不採用案: System writeを既知identityと後続root noticeへ保留相関する案を実装・検証したが、独立レビューでMedium 1件となった。同一file・時間窓は証明できても、Job外processがmemory-map等で生成したdirty pageをCache Managerが書き戻す場合の発生元を証明できず、rootへ誤帰属し得るため採用せず全変更を撤回した。試作binary `70177cb1...`はリリースpin・候補に使用しない。
- 最終是正: WAV temporaryは`open("wx")`成功直後にcleanup対象へ登録し、同じ`FileHandle`で`writeFile → sync → close`を完了した後だけcreate notice・renameへ進む。これにより`fsync`/close失敗時のtemporary残留と、open衝突時の既存file誤削除を同時に防ぐ。System PID 0/4のeventは既知pathでも代理認可せず、診断版native pin `d33c42d3...`のfail-closed条件を維持する。`sync`欠落、各失敗、System writeを拒否するUT/IT/QT oracleを追加した。
- 独立最終レビュー: System代理認可が0件であること、`open("wx")`失敗時の既存file保持、open成功直後のcleanup登録、同一handleのwrite/sync/finally-close、成功時だけのnotice/rename、native pin不変、文書oracleを確認しHigh 0 / Medium 0 / Low 0のPASS。hosted Windows ETWで遅延System writeが消えることだけを隔離runの運用証跡待ちとした。
- Actions run `30493847524`: `fsync`後もrename直後に`F005_ETW_PID_NOT_JOB_MEMBER_SYSTEM_PROCESS_BOUND_FILE_OBJECT`で安全停止した。別FileObjectではなくroot eventで結合済みの同一FileObjectをCache Managerが遅延writeしたが、代理認可は行わず、候補保存step・公開tree変更とも0件。
- 不採用案2: Node.jsのopen flagへ`O_SYNC`を追加する案は、独立レビューでWindowsのNode.js 24.11.0では`fs.constants.O_SYNC`が`undefined`であり、bitwise演算後に通常の`O_CREAT | O_EXCL | O_WRONLY`へsilent fallbackするMedium 1件を確認した。実装・偽陽性となるsource文字列試験・文書主張を全て撤回し、commit/pushしていない。
- 最終是正2: root workerをbreakaway禁止Jobへ収容した後、固定SHAの`f005-guard.exe`を一回の書込み専用childとして起動する。helperはexact keyと最大8,000,000文字のJSONLだけを受け、WAVを1〜5,760,044 bytesへ固定する。canonical rootからparentまでshare-deleteなしのdirectory handleを保持し、`CreateFileW(CREATE_NEW, FILE_FLAG_WRITE_THROUGH | FILE_FLAG_OPEN_REPARSE_POINT)`、同一file handleでwrite、`FlushFileBuffers`、length、全byte readback、SHA-256検証を完了する。衝突時は既存fileを保持し、作成後の失敗時は同じhandleへdelete dispositionを設定する。成功応答とchild終了後だけcreate notice・renameへ進め、FileIOをProcessSequenceNumberで認証できるJob member自身へ帰属させる。System PID 0/4は結合済み/未結合FileObjectとも代理認可せずfail-closedを維持する。
- 初回再現build: SDK `9.0.316`、runtime `9.0.18`、`win-x64` self-contained、固定`PathMap`で2回連続build/verify PASS。初回固定binaryは74,991,452 bytes、SHA-256 `a95ce86b374a49a7767b29a0a9899dc898a6aa5acf494de274058b942e843a3c`、Program SHA-256 `2f9ea2effb4731d5b2eab16d450e6ad8f599b1bccf4c989cdf74b7675cb96d61`。このpinは次の独立レビュー差戻しを受けて最終候補から除外した。
- 初回影響試験: native helperの作成・既存file衝突・SHA不一致・5,760,044 bytes同値・5,760,045 bytes超過を含む重点26試験、型検査、lint、production build、diff-checkをPASS。全71 files / 1,264試験では差分外の大規模copy系4件が時間制限に達したが、該当3 files / 28試験の単独再実行は全PASSし、内容不一致は0件だった。候補branch、accepted audio、公開treeは未変更。
- 独立レビュー差戻し: write成功応答直後にhelperを終了してからcreate noticeを送っていたため、遅延ETW callbackが終了済みchildをJob memberとして認証できないMedium 1件を検出した。また、成功応答後close失敗時のcleanup登録前残留と、汎用native modeが他operation・8,000,000文字入力を受理する文書境界差をLow 2件として検出し、commit/pushを停止した。
- lifecycle是正: nativeに`--write-through-once`専用modeを追加し、通常modeの65,536文字上限を維持したまま、専用modeを`hello → write-through → write-commit/write-abort`の一回だけへ制限した。write/readback後もchild、file、rootからparentまでの全handleを保持し、create noticeのETW認証が返った後にだけexact path/SHA付きcommitでhandleをcloseする。notice失敗、abort、commit前EOFは同じfile handleへdelete dispositionを設定する。Nodeはwrite成功直後にtemporaryをcleanup対象へ登録し、commit・helper終了後だけrenameするため、遅延callback raceと成功応答後close失敗の残留を同時に除去した。
- PID結合是正: 独立再レビューで、helper childのETW観測に対してnoticeがnamed-pipe peerのroot PIDへ固定され、正常系が常に未相関となるMedium 1件を検出した。helper `hello`の実PIDをwrite leaseとcreate noticeへ結合し、native側はroot peerの認証を維持したまま、notice producerが現在のprivate Job memberであることを確認する。相関はproducer PID完全一致に加えて、ETW認可時のQPC birth fence・保持process handle・ProcessSequenceNumber一致を従来どおり必須とし、任意のJob外PIDや別process世代をnotice入力だけで認可しない。
- 強制終了cleanup是正: `CREATE_NEW`後・成功reply前のkill/crash/stdout断はmanaged abortへ到達しないLowを残していた。backendはhelper terminate後のexitをawaitしてhandle closeを確定し、外側生成処理は自身が`mkdir(recursive:false)`で排他的に作成したstaging rootを失敗時に再帰回収する。既存・共有・公開directoryは対象にせず、WAV上限5,760,044 bytesと追加audio上限100 MiBも維持する。応答前にbounded tempが残るfaultを投入し、root不在までの回収を動的試験へ追加した。
- process世代結合是正: PID結合後の独立レビューで、notice時のJob member確認と過去ETW観測がPIDだけで相関し、同一Job内のPID再利用で別世代を取り違え得るMedium 1件を検出した。Nodeはnative `hello.processId`を実spawn PIDと一致させる。nativeはnotice時producer handleから`ProcessSequenceNumber`を取得し、ETW event認可時にも同sequenceを観測・deferred renameへ内部保持する。既存観測・後着観測・deferred renameの全相関をPID＋sequence完全一致へ変更し、journal外部schemaは変えない。
- cleanup identity是正: `terminateAndWait`は`exit`だけを成功とし、`error`やkill失敗をrejectする。staging rootは作成直後のdirectory種別・非symlink・dev/ino・realpathを保持し、再帰cleanup直前に全てを再照合する。同名directory置換時は削除せず元の失敗を維持する。
- lifecycle是正後の再現build（後続hardening前の実測値訂正）: 固定binaryは74,995,548 bytes、SHA-256 `b23b7181445fe94acec62ff1bbb2d8f1eddb5b51594e891e42062c02b3dfbddc`、Program SHA-256 `e8ce7d540226a5df59b4eef36a9a402580bda02be0302a68537b9c74c669f906`。SDK/runtime/RID/PathMap固定で2回連続build/verifyをPASSした。
- lifecycle影響試験: 通常/専用mode隔離、commit前rename拒否、commit永続化、abort・EOF回収、notice失敗時abort、応答前orphan回収、producer PID結合、上限同値/+1を含む重点27試験、型検査、lint、production build、diff-checkをPASSした。全71 files / 1,265試験では高負荷時に差分外のcopy/build系5件がtimeoutまたはVite plugin timing警告で停止したが、該当4 files / 81試験の単独再実行は全PASSし、内容不一致は0件だった。候補branch、accepted audio、公開treeは未変更。
- recursive cleanup差戻し: staging rootのdev/ino/realpath再照合後に`rm(root,{recursive:true})`する方式は、照合とpathname mutationの間にrootを置換できるため、第三者treeを削除し得るTOCTOU Mediumとして不採用にした。Nodeのtemporary/final pathname deleteとroot再帰削除を全廃し、同名replacement・置換root・第三者entryは削除しない。
- atomic delete-on-close是正: helperは`CreateFileW(CREATE_NEW, FILE_FLAG_WRITE_THROUGH | FILE_FLAG_DELETE_ON_CLOSE | FILE_FLAG_OPEN_REPARSE_POINT)`で作成と回収予約を原子的に結合する。write/flush/readback後もdelete-on-closeを維持した同一handleでtemporaryからfinalへrenameし、create/rename両noticeのETW認証後だけ`FileDispositionInfoEx(FILE_DISPOSITION_ON_CLOSE、DELETEなし)`で解除してcommitする。abort、EOF、応答前kill、rename後killは現在名の同一native identityだけをclose時削除する。commit後の後続失敗はreplyのnative identityを期待値にsame-handle deleteし、root自体はfail-closedで残す。
- Windows契約・最終再現build: NTFS実機で後付け`DELETE | ON_CLOSE`が`ERROR_NOT_SUPPORTED (50)`、旧delete disposition後のrenameが`ERROR_ACCESS_DENIED (5)`となることを確認し、作成時delete-on-closeを維持したhandle rename、rename後EOF回収、`ON_CLOSE`解除commit、応答前強制kill回収をPASSした。専用protocolはrename前commitと2回目renameも拒否する。固定binaryは74,995,548 bytes、SHA-256 `3cf770a52e5ff6115da5722ee260506db6be8fd1a9cfc9c9dc12cb682b518263`、Program SHA-256 `6739f719e71c9bdc026b9debc82c7ce6c02f92fed1109010a3a878ce962d2e69`。固定SDK/runtime/RID/PathMapで2回連続build/verifyをPASSした。
- 最終影響試験: native helperのatomic delete-on-close、同一handle rename、commit/abort/EOF/応答前kill、native identity CAS cleanup、root置換保護、producer世代相関を含む重点3 files / 44試験をPASSした。型検査、lint、diff-checkもPASSし、候補branch、accepted audio、公開treeは変更していない。
- 独立最終再レビュー: 作成時atomic delete-on-close、同一handle rename、rename notice後だけのcommit解除、abort/EOF/応答前kill/rename後killのexact回収、commit済みartifactのexpected native identity付きsame-handle cleanup、置換root・第三者tree保持、PID＋ProcessSequenceNumber相関、全pin同期を実物と実動試験で確認し、High 0 / Medium 0 / Low 0のPASSとした。
- push前広域回帰: production build 697 files / 229,936,251 bytesをPASSした。全71 files / 1,265試験は1,261件PASS、差分外のcopy/recovery系4件だけが高負荷時timeoutとなり、該当4 files / 94試験の単独再実行は全PASSした。内容不一致は0件で、public/data差分も0件を維持した。
- Actions run `30499886153`: 固定native build、固定VOICEVOX取得までPASSし、production pipeline内で安全停止した。候補保存・pushはskipされ、公開treeも未変更。未認証APIではjob log取得が403となるため、次runからallowlist済みの最終進捗、固定failure name/codeだけをGitHub error annotationへ出す。path、stack frame、例外message、secret、自由文はannotationへ含めない。
- Actions run `30500241810`: 同じ境界で安全停止し候補保存・pushをskipした。file指定なしのworkflow commandは公開Check annotation APIへ載らなかったため、workflow自身の固定path/lineを指定するerror annotationと、同一allowlist文字列だけのJob Summaryを併記する。診断関数は固定進捗・name/codeだけを抽出する実行試験をPASSした。
