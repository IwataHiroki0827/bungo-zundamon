---
status: Approved
feature: F009
title: 夢野久作3作品追加 関数設計
version: 1.0.0
updated: 2026-08-24
---

# 関数設計書 F009（夢野久作3作品追加）

## 0. 再利用方針

F009は「新規作者1人・3作品追加」という構造がF006〜F008と同型であり、F005が導入したETW常時監視・native guard機構は使わない。CHG-F005-072以降に確立した「phase前後の実測差分による事後検証」と、F001〜F008実績の汎用release経路は、値がfeatureに依存しない汎用実装であるため直接再利用する。

以降の各`FUN-F009-*`は、DESごとの追跡用論理IDである。多くは実装として新規コードを書かず、既存関数をそのまま呼び出す（本文に既存関数名を明記する）。値（authorId・workId・release commit・catalog SHA・既存作者画像枚数・注記対象2作品・gaiji出現位置等）がfeatureごとに異なるために既存モジュールを直接呼べないもの（baseline pin・author/work registry・source/entity/gaiji抽出・薄い受入ラッパー・catalog統合・route導出・artwork provenance、計5ファイル）だけを新規コードとして定義する。新規コードは既存命名規則（`src/content/f009-*.ts`・`scripts/f009-*.ts`）に限定し、F001〜F008が依存する共有汎用モジュールは一切変更しない。実装フェーズでは、再利用する既存関数のJSDocへ`@des DES-F009-xxx @fun FUN-F009-xxx`を追記する。

## 1. 候補・baseline・作者identity

### FUN-F009-001: F009承認候補registryのvalidate・atomic書込み・manifest生成 [DES-F009-001]

新規コードなし（設定データのみ追加）。既存`validateBatchCandidateRegistry`・`writeBatchCandidateRegistryAtomic`（`src/content/batch-candidate.ts`）でF009候補のexact keys・author/work/approvalBinding・重複・URL/path安全性を検証し、expected old SHA・canonical JSON・temporary file・atomic rename・post-read hashで`content/batch-candidates.json`へ追記する。`content/batch-definitions/F009.json`・`content/approval-policies/F009.json`を新規作成し、そのSHA-256を`BATCH_DEFINITION_REFS`・`APPROVAL_POLICY_REFS`（`batch-candidate.ts`）へF004/F006/F007/F008と同じ形式で追記登録する。既存`loadAndVerifyClosedApproval`（`answer === '承認'`または`approval_answer_normalized === '承認'`を承認済みとして受理）で`docs/evidence/requirements/F009-approval-binding.json`のqueue item hashとApproved SRS-F009/QT-F009実体を照合した`VerifiedClosedApproval`を得て、既存`loadAndVerifyBatchCandidate` → `createBatchManifestFromApprovedContext`（F004/F006/F007/F008が使う経路）で一致した1候補だけを`content/batches/F009/batch.json`の`draft`へmaterializeする。F009向けの新規application exportやID分岐は作らない。

### FUN-F009-002: loadPublishedV080Baseline(workspace, options?) -> Promise<PublishedV080Baseline> [DES-F009-002]

`f008-baseline.ts`（`F008_V070_PINS`／`loadPublishedV070Baseline`）と同型の専用pinモジュール`f009-baseline.ts`に新規定義する。内部定数`F009_V080_PINS`はrelease commit `bddf37c7d25ff57fb6ecde9d6bc8a1f06139f197`、tag `v0.8.0`、catalog SHA-256 `b46bfd5dd78d0ac6b343a2ee202acec901dc1394e8752df3129de2f3dc66e5cc`、dist SHA-256 `842042cfc8683fa7aa1d7e4aa3e38034b6d90360b093b7d4a536f75afbafc330`、production artifact SHA-256 `c7ec3229764bcb3e1c9ea221b96efcababb7ead44b62666771b27b37c0877c04`のexact値を持つ（pf-implement着手時に`docs/evidence/release/F008-smoke.json`・`F008-deployment.json`の実測値で再確認する）。`git show`相当のread-only adapterで固定Git objectと固定hashのbaseline descriptorから7作者・24作品・1199台詞、全path/bytes/SHA/referenceを復元する。候補checkoutの現行publicからdescriptorを生成しない。単独assetのpath・bytes・SHA-256のexact比較、追記可能な集約manifest（`content/catalog.json`）の固定7作者24作品entry canonical projection一致は読込と同時に検証する。F009混入、欠損、reparse、hash差、既存entry差分は`F009_PUBLISHED_BASELINE_MISMATCH`で停止し、修復はしない。

### FUN-F009-003: isMintedPublishedV080Baseline(value) -> value is PublishedV080Baseline [DES-F009-002]

`f008-baseline.ts`の`isMintedPublishedV070Baseline`と同型のbrand検査関数。`load`関数と`isMinted`brand検査だけを提供し、tree/catalog/dist不一致の実体比較を`load`内部で完結させる構造をそのまま踏襲し、別立ての`verifyInvariant`関数は新設しない。work preview・統合tree・distの各段階で同一固定bundleを`isMintedPublishedV080Baseline`で再検証してから使う。

### FUN-F009-004: defineF009AuthorAndWorkRegistry() -> F009WorkRegistry / verifyF009AuthorIdentity(registry, baselineCatalog) [DES-F009-003]

`f009-baseline.ts`または新規`f009-source.ts`のmodule-private定数として、`authorId=000096, originalName=夢野久作, name=ゆめのきゅうさく, slug=yumeno-kyusaku`と、`002381`（瓶詰地獄, order 1）→`046694`（きのこ会議, order 2）→`002380`（死後の恋, order 3）のexact work tupleを保持する。既存`BatchAuthor`のcanonical fieldsから`identitySha256`を再計算し、F009候補registry（FUN-F009-001）とcanonical candidateのauthor/work記述がこのregistryと一致することを検証する。

F007・F008実装で確認済みの通り、`verifyExistingAuthorIdentity`（`batch-candidate.ts`）は`authorExpectation: 'reuse'`のContextしか受理しない。F009の新規作者導入（`authorExpectation: 'introduce'`）には、F006〜F008の`verifyF00NAuthorIdentity`と同型の自己完結型検証関数`verifyF009AuthorIdentity(registry, baselineCatalog)`を`f009-source.ts`内に実装し、DES-F009-002のbaseline join 0件・他7作者との非衝突をこの専用関数で検証する（`verifyExistingAuthorIdentity`は使わない）。3作品全てのauthorIdが`000096`の場合だけ`authorMode=introduce`を許可する。汎用CAS遷移（`pending → extracted → reviewed → budget-approved → voiced`）は既存`transitionWorkState`（`batch.ts`）をそのまま用いる。不一致は`F009_REGISTRY_MISMATCH`。

## 2. 権利・原典・抽出・レビュー

### FUN-F009-005: F009原典・書誌の独立snapshot取得と固定 [DES-F009-004]

F006〜F008実装で確認済みの通り、`f005-source.ts`〜`f008-source.ts`はいずれもauthorId/workId定数・`mintedSourceRecords`（module-private WeakSet）brand検査により該当featureへhardcodeされており、外部モジュールから他featureのworkIdを渡して呼び出すことはできない。F002〜F008が機能導入ごとに専用の`-source.ts`を持つ既存慣習に倣い、**新規`src/content/f009-source.ts`をF009専用に実装する**。`f008-source.ts`の構造（型定義・`ProductionAozoraTransport`によるHTTPS exact allowlist取得・権利判定ロジック・書誌固定・`normalizeF008AozoraXhtmlEntities`/`extractF008DialogueCandidates`と同型のmodule-private brand検査）を忠実に踏襲しつつ、`authorId=000096`・`F009_WORKS=[002381(瓶詰地獄,order1), 046694(きのこ会議,order2), 002380(死後の恋,order3)]`へパラメータ化する。共通化できる箇所（HTTPS transport自体、`ProductionAozoraTransport`/`ProductionPolicyTransport`の既存feature非依存export）はそのまま再利用し、F009固有型・定数だけを複製する。公式拡充CSV・作家ページ・図書カード・XHTML（`2381_13352.html`／`46694_27682.html`／`2380_13349.html`）を取得し、権利・VOICEVOX/ずんだもん規約の`decision: allow`判定、書誌・`SourceRecordV2`相当型（proofreaderは3作品とも記載ありのため非null値のみ使用）を固定する。差分・未判定・transport異常はfail-closed。

### FUN-F009-006: F009未定義entity・gaiji検出・決定的抽出・長大候補分割 [DES-F009-005][DES-F009-015]

新規コードなし方針だが、FUN-F009-005と同じ理由でmodule-private brandに依存するため、`f009-source.ts`（FUN-F009-005で新設）へ同型の関数（`collectF009SourceSnapshot`・`normalizeF009AozoraXhtmlEntities`・`extractF009DialogueCandidates`等）として実装する。内包する未定義named entity検出preflightを選定時・公開直前の独立snapshotで実行する。固定原典のexact XHTML 1.1外部DOCTYPE宣言だけをresolverなしで許し、internal subset・他DOCTYPE・ENTITY・XInclude・外部schema/stylesheetを拒否したうえで、候補ID・順序・表示文を決定的に生成する。同一入力2回のcanonical hash不一致は`EXTRACTION_NON_DETERMINISTIC`。

**gaiji要素の候補内非混入検証（DES-F009-005の新規論点）**: `f009-source.ts`へ次の2関数を新規実装する。

```ts
function detectF009GaijiElements(rawText: string): readonly { start: number; end: number }[];

function verifyNoGaijiWithinCandidates(
  candidateSet: F009CandidateSet,
  gaijiRanges: readonly { start: number; end: number }[],
): void;
```

`detectF009GaijiElements`は正規表現`/<img\b[^>]*\bclass\s*=\s*"gaiji"[^>]*>/gu`相当でraw XHTML text中の`<img class="gaiji">`要素のbyte offset範囲を全件収集する（属性順序に依存しないよう`class`属性値の完全一致だけを条件とする）。`verifyNoGaijiWithinCandidates`は`extractF009DialogueCandidates`が生成した各candidateの`rawTokenRange`と`gaijiRanges`の区間重なりを線形走査で判定し、1件でも重なりがあれば`F009_GAIJI_WITHIN_CANDIDATE`で該当作品を`pending`に留める。両関数は選定時（`collectF009SourceSnapshot`直後）と公開直前（`rehydrateF009PredeploySnapshot`相当、後述）の独立snapshotの双方から呼び出し、いずれか一方でも省略しない。DOMAIN-F009.md §5の選定時実測（瓶詰地獄4件・死後の恋2件のgaiji要素、いずれも候補外）を実装時に再現することを期待値とする。既存の`normalizeSpeechText`（`processing.ts`）の`/[〓※�]/u`検査は無変更のまま適用され続け、`verifyNoGaijiWithinCandidates`はこれを置き換えるものではなく、raw XHTML段階での追加の防御層として並行して働く。

**F007/F008由来の長大候補分割（DES-F009-015）の実発動**: `extractF009DialogueCandidates`へ`splitOverlongF009Candidates`を、F007/F008の`splitOverlongF00NCandidates`と同一アルゴリズム・同一閾値（600文字、句点「。」の文境界でのみ分割、分割後order 0..N-1再採番、`sourceAnchor`/`rawTokenRange`/`rawSourceSha256`は分割元と同一を維持）で実装する。DOMAIN-F009.md §3.1が実測した死後の恋の1候補（正規化前1,748文字）に対して実装フェーズの実測で発動する見込みが高い。**F007・F008では対応するテストfixtureが実候補で分割を発動させたことがなく、分割ロジックの複数piece生成・order再採番パスは主にunit testの合成入力でしか検証されていなかった。F009はこの分割ロジックが初めて実データ（実際にAozoraから取得したXHTML由来の候補）に対して発動する機会であるため、`f009-source.test.ts`には実際の死後の恋raw XHTML（またはその該当箇所を抽出した固定fixture）を用いた分割検証caseを追加する。**分割処理自体の決定性は別途二重実行で検証する。`scripts/f009-prepare-editorial.ts`の`speechCorrections`適用ロジックも、F007/F008と同様に分割後の複数candidateが同一`sourceAnchor`を共有するケースに対応させる（「宣言された各補正は合計ちょうど1回だけ適用される」不変条件を検証）。

設計判断: F002〜F008が依存する共有汎用モジュール（`src/voice/generation.ts`・`src/voice/client.ts`・`src/content/processing.ts`・`src/content/batch-production.ts`）は変更せず、F009ローカルの抽出層（`f009-source.ts`）だけで完結させる方針を採用した（F007・F008と同一の設計判断）。

### FUN-F009-007: F009独立二重判定・裁定・回復 [DES-F009-006]

新規コードなし。既存`registerEditorialAuthorizations`・`sealAndValidateEditorialJudgmentSet`・`reconcileIndependentJudgments`・`verifyEditorialCompleteness`・`loadAndVerifyEditorialJudgmentSets`・`recoverEditorialJudgmentTransaction`（`src/content/editorial-independent.ts`、F002〜F008が使用する汎用モジュール）をそのまま用いる。`candidateSetSha256/policySha256/promptSha256/toolSha256/nonce`を含む一回限りのauthorizationで判定を封印し、不一致digestが確定した場合だけ第三裁定authorizationを発行する。最終`pending: 0`まで音声stageへ渡さない。死後の恋の長大候補分割（FUN-F009-006）で生成される各piece candidateは、分割後にFUN-F009-006が確定したcandidate setの一部として本関数群へそのまま渡る（本関数側に分割固有の分岐は不要）ため、二重判定はpiece単位で自然に個別実施される。

### FUN-F009-008: F009読み上げrevisionの適用 [DES-F009-007]

新規コードなし。既存`applySpeechRevisions`（`src/content/f003-reuse.ts`、F003以来再利用されている汎用関数）でdisplay textを変更せず、連続revisionのbefore/after/reason/hashを照合して最終speech textを作る。飛び越し・循環・別candidate混入は既存code`SPEECH_REVISION_CHAIN_INVALID`。

## 3. 音声・容量・受入

### FUN-F009-009: F009差分音声生成・容量forecast・write健全性事後検証 [DES-F009-008]

新規コードなし。単一候補安全性・差分音声生成は既存`planVoiceDiff`・`authorizeVoiceDiffPlan`・`generateVoiceDiff`・`verifyVoiceCompleteness`（`src/voice/generation.ts`）をそのまま用いる。固定VOICEVOX tuple（ENGINE `0.25.2`、speaker UUID、style ID `3`、style名`ノーマル`、24 kHz/mono/16 bit PCM）はF002以来の既存configをF009向けにcanonical複製する。分割後piece candidateも既存`planVoiceDiff`から見れば通常のcandidateと区別なく扱われ、piece単位で個別に安全性判定・生成・completeness検証が実施される。容量forecastは既存`forecastCapacity`・`measureGitRepository`・`verifyActualCapacity`（`src/voice/budget.ts`）を用い、追加WAV・Pages artifact・source repository・単一Git object・作業ドライブの各区分をbyte単位で判定する。書込み健全性は既存`scanF005Workspace`・`foldF005Declarations`・`verifyF005Postconditions`・`writeF005TemporaryFile`（`f005-postcondition.ts`・`f005-postcondition-write.ts`）をそのまま呼び出す。native guardやETW常時監視は新設しない。いずれか不成立ならfail-closedとし、raw pathを出さない固定codeで停止する。

### FUN-F009-010: F009作品単位atomic受入・回復 [DES-F009-009]

F006〜F008実装で確認済みの通り、汎用`promoteVerifiedWorkArtifacts`の`assertInputs`は`voiced`stageの`outputHashes`に直接evidence値を要求するが、F006〜F008が確立した「間接ハッシュ規約」（`voiceEvidenceRef`ファイルのSHA-256だけを格納する方式）はF009でも同一パターンで踏襲する。F006〜F008と同じパターンで、**F009専用の薄いacceptanceラッパー`src/content/f009-acceptance.ts`を新設する**（既存`promoteVerifiedWorkArtifacts`・`batch-production.ts`の`recoverBatchSourceArtifactPromotion`・`writeBatchManifestAtomic`は変更せずそのまま呼ぶ薄い橋渡し層とする）。

瓶詰地獄→きのこ会議→死後の恋の順にだけ`pending → extracted → reviewed → budget-approved → voiced → accepted`へ進める。`f009-acceptance.ts`は、F009の間接ハッシュ規約から`promoteVerifiedWorkArtifacts`が要求する直接evidence値を再読込・再計算し、caller作成の`pass`値を信用せずcanonical allowlist pathから実体を検証したbranded evidenceを組み立てたうえで`promoteVerifiedWorkArtifacts`を呼ぶ。exclusive lock・expected manifest/tree hash・random staging・journal・file sync・atomic rename・postwrite hash・bounded retryは既存契約をそのまま継承する。process停止後は既存`recoverBatchSourceArtifactPromotion`（`batch-production.ts`）および`promoteVerifiedWorkArtifacts`内部journalから旧版または完成新版へ決定的に回復し、第三者変更を上書きしない。manifest書込みは既存`writeBatchManifestAtomic`（`batch.ts`）を用いる。

## 4. 公開UI・権利表示・安全設計

### FUN-F009-011: mergeNewAuthorCatalog009(baselineCatalog, fragment, verifiedAuthor) -> F009WorkPreviewCatalog | F009FinalCatalog [DES-F009-010]

`f008-catalog.ts`の`mergeNewAuthorCatalog008`と同型の統合関数を新規`f009-catalog.ts`に定義する。baseline join 0件、呼出前提の`baselineCatalog.authors.length === 7`・`baselineCatalog.works.length === 24`（v0.8.0）、verified author/work/asset ID非衝突を検証し、既存7作者24作品のserialized projectionをexact維持したまま夢野久作authorと3作品を末尾追加する。統合後は`catalog.authors.length === 8`、`catalog.works.length === 27`を必須とする。`batches`配列へ`{batchId: 'F009', feature: 'F009', authorId: '000096', workIds: [...]}`を追加する。既存projection差分は`F009_CATALOG_MERGE_CONFLICT`。

**2作品同時の作品固有注記の結線**: 既存`work-notices.ts`の`WorkNoticeRegistry`/`WorkNotice`/`WorkNoticePlacement`型と`WORK_NOTICE_TEXT`定数（authorId-parameterized）をそのまま再利用する。`TRUSTED_REGISTRY_BINDINGS`（同ファイル、frozen object literal）へ**`authorId: '000096'`の新規entry(1件)をデータとして追加する**（`work-notices.ts`のロジック・既存F003/F007/F008 entry・関数シグネチャは一切変更しない）。新規entryの`path`は`content/batches/F009/work-notices.json`、`sha256`は実装時に確定するcanonical JSONのSHA-256、`sourceFacts`は瓶詰地獄（`officialContentWarning: true, unfinished: false`）・きのこ会議（`officialContentWarning: false, unfinished: false`）・死後の恋（`officialContentWarning: true, unfinished: false`）の3件とする（F008と同型の「3件中2件がtrue」の混在パターン、`sourceFacts`は既存のreadonly配列型のためこの混在をスキーマ変更なしで表現できる）。`content/batches/F009/work-notices.json`自体は瓶詰地獄・死後の恋に`notices: [{textKey: 'dialogue-excerpt-scope', placements: [...]}, {textKey: 'official-content-warning', placements: [...]}]`、きのこ会議に`notices: [{textKey: 'dialogue-excerpt-scope', placements: [...]}]`だけを持たせる。

`f009-catalog.ts`（新規、F008の`f008-catalog.ts`と同型だがF009専用）は`loadAndValidateWorkNotices(workspace, '000096')`（既存関数、無変更）を呼び、返る`WorkNoticeReport.works[].completionStatus`/`.notices`を各workのCatalog fragmentへF003/F007/F008と同じ方法でjoinする。これにより新規application source分岐（if/switch等の条件分岐コード）を一切追加せず、データ（登録entry・registry JSON）だけでF009固有の注記表示を実現する。公開直前検査で図書カード「作品について」欄の差分（削除・新設・文言変更）が見つかった場合はfail-closedで当該作品を`pending`に留める。

### FUN-F009-012: deriveF009RouteSet(catalog, staticRoutes) -> F009ExpectedRouteSet [DES-F009-010]

`f008-catalog.ts`の`deriveF008RouteSet`と同型。`catalog.authors.length === 8`、8作者slugが重複なしかつ既存slug regexに一致することを検証し、`#/`、既存7作者route（`akutagawa-zunnosuke`/`miyazawa-zunji`/`dazai-osamu`/`natsume-soseki`/`nakajima-atsushi`/`mori-ogai`/`edogawa-ranpo`）、`#/authors/yumeno-kyusaku`、`#/favorites`、`#/credits`のexact 11 routeを導出する。手書きallowlistへ二重定義せず、重複・不足・余分・未知slugは`F009_ROUTE_SET_INVALID`。

### FUN-F009-013: F009初期全閉・単一音声・お気に入り契約の維持 [DES-F009-011]

新規コードなし。既存`AudioController`（`src/ui/audio-controller.ts`）と既存お気に入りstore（`bungo-zundamon:favorites:v1`）を無変更で再利用する。全dialogueとの1対1 control、`aria-pressed`、自動再生0、ユーザー操作前の音声取得0、同時再生1、route・作者・台詞切替時のpause/currentTime=0/src解除、古いevent世代の無視は既存実装のまま維持され、夢野久作route・3作品の追加、2作品の注記表示（既存の`notices`描画コンポーネントがF003/F007/F008で既に使用済みのためF009向けの新規UIコードは不要）はデータ駆動で吸収されるため新規分岐を要求しない。保存IDのplain JSON exact schema検査、prototype/getter汚染防御も既存のまま。

### FUN-F009-014: sealF009ArtworkProvenance(generation, inputs, finalImage) -> ArtworkProvenanceF009 [DES-F009-012]

F006〜F008実装で確認済みの通り、`src/notices/artwork-provenance.ts`はOpenAI内蔵image_genツール専用の固定検証であり、ComfyUI等の他生成器を使う場合は使えない。F009は`src/notices/artwork-provenance.ts`を一切変更せず、**F009専用の軽量self-contained schema**を新規`f009-artwork.ts`に定義する（F006〜F008の`f00N-artwork.ts`と同型）。

`sealF009ArtworkProvenance(generation, inputs, finalImage)`は、exact final image path（`content/batches/F009/public-files/artwork/yumeno-kyusaku-zundamon.png`、公開先パスはDD確定後の実装時に画像生成器・実際の出力名に合わせて確定する）、参照入力exact `[]`（zero input/reference images）、実際の生成情報（`generator`・`generatorVersion`・`model`・`workflow`・`prompt`・`seed`等、捏造せず実値のみ）を記録する`ArtworkProvenanceF009`型（新規定義、`ArtworkManifestIdentity: {batchId:'F009', authorId:'000096', outputPath}`を持つ）を返す。PNG decode・生成原本SHA・最終SHAの一致検証は既存のfeature非依存pure関数`verifyPngjsLockfileIntegrity`・`computeDHash64V1`（`f005-artwork.ts`、png bytesのみを引数に取る）をそのまま呼び出す。schema不正は`F009_ARTWORK_PROVENANCE_INVALID`。

既存8作者画像との比較（v0.8.0時点で7件、F009生成後8件目を追加）は`verifyF009ArtworkAgainstCatalog`（下記FUN-F009-015）が既存`computeDHash64V1`の再計算値でHamming距離8以下を`F009_ARTWORK_NEAR_DUPLICATE`として拒否する設計を、**F008がF006由来のexact-countバグ（`existingArtwork.length !== N`、2026-08-22 commit `cd5ecc8`で`< N`へ緩和）の教訓を踏まえ実装当初から`existingArtwork.length < 6`で実装した前例をさらに踏襲し、F009は`sealF009ArtworkProvenance`・`verifyF009ArtworkAgainstCatalog`の実装当初から`existingArtwork.length < 7`（下限判定）で実装する**。将来のF010統合時に既存作者数が8に増えても再修正不要となる。

### FUN-F009-015: verifyF009ArtworkAgainstCatalog(provenance, finalImage, existingArtwork) -> ArtworkAcceptance [DES-F009-012]

FUN-F009-014と同じ理由により、既存のOpenAI専用機構は使わない。`f009-artwork.ts`に自己完結型の検証関数として新規実装する。既存7作者画像（`existingArtwork.length < 7`は不正=下限違反として拒否、FUN-F009-014の設計判断を参照）とのbyte一致、または既存`computeDHash64V1`で再計算したdHash64のHamming距離8以下を`F009_ARTWORK_NEAR_DUPLICATE`として拒否する。既存画像の自己申告SHA/dHashを実体から再計算して照合し、authorId `000096`が既存7作者と衝突しないことを検証する。

## 5. セキュリティ・release・継続計画

### FUN-F009-016: F009セキュリティ検査・段階release [DES-F009-013]

新規コードなし。phase差分対象の全pathは既存`resolveSafeWorkspaceFile`（feature非依存）でreparse point非該当・hardlink数1・identity一致を再検査する。既存security checker・offline Pages builder（frozen lockfile、build時network 0、Actions最小権限、`persist-credentials:false`、`uses:`40桁commit SHA pin）をそのまま再利用する。secret scanはtracked/untracked release入力・workflow・生成dist・upload artifact manifestを対象に値を記録せず種類・件数だけを証跡化する。Playwright既存project・既存test IDへF009必須case（`#/`、既存7作者route、`#/authors/yumeno-kyusaku`、`#/favorites`、`#/credits`のexact 11 route、画像200、local/hosted WAV Range 206、2作品の注記表示）を追加してskip 0で通す。F008で確立した「実装フェーズの一部として専用e2e spec（`tests/e2e/f00N-*-author.spec.ts`パターン）を最初から作成する」方針を継続し、`tests/e2e/f009-yumeno-author.spec.ts`を実装フェーズの一部として最初から作成する。

公開はF001〜F008実績の汎用release経路（既存script`scripts/verify-project.mjs`によるprecheck、ゲート④承認後のmainへのfast-forward、既存`recordPublishedBatch`（`batch.ts`）によるexclusive lock・expected old/new SHA・journal・file sync・atomic rename・post-read検証、`PAGES_DEPLOY_ENABLED`/`PAGES_DEPLOY_COMMIT`ゲートでの1回限り有効化、deploy変数read-back falseの確認）をそのまま用いる。公開後スモークが失敗した場合はv0.8.0のexact 10 routeへrollbackする。

### FUN-F009-017: F009継続順位計画の更新 [DES-F009-014]

新規コードなし。既存`AuthorExpansionPlanV1`（`baselineAuthors=3`、`targetAdditionalAuthors=7`）を既存`advanceAuthorExpansionPlan`（`src/content/f005-foundation.ts`、feature非依存の汎用関数）でF009完了時点（追加5人目、残2人、最終上限10作者）へ遷移する。知名度scoreは既存`computeAuthorPopularityRanking`が2022年公式XHTML上位500行と拡充CSVの`著者`結合から再生成し、DOMAIN-F006.mdで確定済みのSHA-256`580164841c4ade8bc41862bc1ef295e92fba5b20f4b3907e04c95e972176b423`と一致することを公開直前に確認する。author identityの重複を拒否し、暫定順位の書き換えは行わない。上限+1、score書換え、同一author再採用は既存code`F005_AUTHOR_PLAN_LIMIT`のまま用いる。

## 6. DES→FUN対応表

| DES | FUN |
|---|---|
| DES-F009-001 | FUN-F009-001 |
| DES-F009-002 | FUN-F009-002, FUN-F009-003 |
| DES-F009-003 | FUN-F009-004 |
| DES-F009-004 | FUN-F009-005 |
| DES-F009-005 | FUN-F009-006 |
| DES-F009-006 | FUN-F009-007 |
| DES-F009-007 | FUN-F009-008 |
| DES-F009-008 | FUN-F009-009 |
| DES-F009-009 | FUN-F009-010 |
| DES-F009-010 | FUN-F009-011, FUN-F009-012 |
| DES-F009-011 | FUN-F009-013 |
| DES-F009-012 | FUN-F009-014, FUN-F009-015 |
| DES-F009-013 | FUN-F009-016 |
| DES-F009-014 | FUN-F009-017 |
| DES-F009-015 | FUN-F009-006 |
